# Tasks: usuario-mobile-consumo — consumption tracking screens + the `core-api` read surface they require

Expands `proposal.md` (D1-D7, locked) and `design.md` (D-1 through D-10, locked) into a chained, test-first checklist. Order and hard constraints below are `design.md`'s, not re-derived here.

**Hard ordering (from `design.md`'s own "Migration / Rollout", not re-opened):** `@repon/types` -> core-api endpoints -> `@repon/auth` helper -> screens. Within core-api, ports/adapters/domain math (PR2) must land before any use case that calls them (PR3, PR4). Within screens, shared presentational pieces (PR6a) land before the tab that composes them (PR6b); the tab (PR6b, D-7/D-8/D-9) lands before the two create forms and the two read-only screens, all four of which reuse `<OwnerTabs>`. Doc deltas (PR10) land last, describing shipped behaviour, not aspirational behaviour.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~3,000-4,200 total, across **12** chained PRs; per-PR range 60-420 |
| 400-line budget risk | **High** — PR3, PR4, PR6b and PR7b each run 320-420 in the upper estimate (list/adherence endpoints carry a use case + DTO + mapper + controller route + module wiring + one e2e cross-tenant spec each; the tab and the pet create form each carry a long RNTL testing-strategy row per `design.md`) |
| Chained PRs recommended | Yes — 3 new endpoints + 3 port/adapter methods + domain math + a new shared-package helper + 5 screens + 6 shared components + e2e/RNTL coverage, no single-PR path stays under 400 |
| Suggested split | PR1 -> PR2 -> {PR3, PR4} -> PR5 -> PR6a -> PR6b -> {PR7a, PR7b} -> PR8 -> PR9 -> PR10 (see diagram) |
| Delivery strategy | `ask-on-risk` (cached this session) |
| Chain strategy | **stacked-to-main — user-confirmed**, matching this project's existing precedent (`mobile-auth-login`, `catalogo`, `refill-matching`, `ofertas`, `pedidos-pagos` all used it) |

```text
Decision needed before apply: Resolved — stacked-to-main
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

### Per-PR estimate

| PR | Scope | Est. lines | Risk | Depends on |
|---|---|---|---|---|
| 1 | `@repon/types` — consumo view types (D-2, D-5) | 60-90 | Low | none |
| 2 | Backend ports-out + Kysely adapters + domain pure functions (D-2, D-3) | 300-380 | Medium | none (parallelizable with PR1) |
| 3 | List endpoints — `mis-mascotas` + `mis-consumos` (D-1, D-4, D-5, D7) | 320-400 | Medium-High | 1, 2 |
| 4 | Adherence endpoint — `mi-adherencia` (D-1, D-2, D-4, D-5) | 320-400 | Medium-High | 1, 2 |
| 5 | `@repon/auth` JSON/error helper + Spanish mapping (D-10) | 230-300 | Low-Medium | 3, 4 (rollout order — no hard code dependency) |
| 6a | Shared consumo UI components (D-9, presentational pieces of D-8) | 300-400 | Medium | 1 |
| 6b | `s-consumo` tab wiring + dose marking (D-7, D-8, D-9) | 320-420 | Medium-High | 3, 5, 6a |
| 7a | `s-consumo-nuevo` — self create form (D-5/D-6) | 280-360 | Medium | 1, 5 |
| 7b | `s-consumo-nuevo-pet` — two-step create flow (D-6, D-8) | 320-420 | Medium-High | 1, 5 |
| 8 | `s-consumo-config` — read-only actives list (D-4, D-9) | 180-260 | Low-Medium | 3, 5, 6a |
| 9 | `s-consumo-historial` — 7-day adherence view (D-2, D-9) | 240-320 | Medium | 4, 5, 6a |
| 10 | Declared `SPEC.md` deltas (D2, D4) | 20-40 | Low | 3, 4, 6b, 7a, 7b, 8, 9 |

### Dependency graph

```text
PR1 (types) ────────────────────────────────────┬──────────────┬───────────────┐
                                                  │              │               │
PR2 (ports+adapters+domain calc) ── PR3 (list) ──┤              │               │
                              └──── PR4 (adherencia) ──┐         │               │
                                                        │         │               │
                                          PR5 (@repon/auth helper, rollout-ordered after 3+4)
                                                        │
                                          PR6a (shared components) ──┐
                                                        │             │
                                          PR6b (s-consumo tab) ◄──────┘
                                                        │
                                  ┌─────────────────────┼─────────────────────┐
                              PR7a (nuevo)          PR7b (nuevo-pet)      PR8 (config) ── PR9 (historial)
                                                        │
                                          PR10 (SPEC.md deltas, last)
```

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | `@repon/types` consumo view types | PR 1 | `pnpm --filter @repon/types exec tsc --noEmit` | N/A — type-only package, no runtime; workspace `pnpm typecheck` is the real proof it doesn't break consumers | Revert the 5 new exported types in `packages/types/src/consumo.ts` — unreferenced until PR3/PR4 import them |
| 2 | `findByUserId` x2 + `contarTomasPorDia` + domain adherence math | PR 2 | `pnpm --filter core-api exec jest domains/consumo` | N/A — no HTTP caller until PR3/PR4 | Revert the 3 port-method additions, 3 adapter methods, 4 pure functions, and `ZONA_HORARIA_ADHERENCIA` — the 4 existing routes are untouched |
| 3 | `GET /consumo/mis-mascotas` + `GET /consumo/mis-consumos` | PR 3 | `pnpm --filter core-api exec jest domains/consumo/ports-in/listar` | `pnpm --filter core-api exec jest --config ./test/jest-e2e.json consumo-listar` — real Nest app via supertest, cross-tenant proof | Revert 2 use cases, 1 DTO, mapper addition, 2 controller routes, module registration, e2e spec — `POST` siblings untouched |
| 4 | `GET /consumo/mi-adherencia` | PR 4 | `pnpm --filter core-api exec jest domains/consumo/ports-in/calcular-adherencia` | `pnpm --filter core-api exec jest --config ./test/jest-e2e.json consumo-adherencia` | Revert 1 use case, 1 DTO set, mapper addition, 1 controller route, module registration, e2e spec |
| 5 | `@repon/auth` JSON/error helper + `mensajes-error.ts` | PR 5 | `pnpm --filter @repon/auth test && pnpm --filter usuario-mobile test` | N/A — no screen calls it yet until PR6b/PR7a/PR7b | Revert `packages/auth/src/api-json.ts`, its barrel export, and `apps/usuario-mobile/lib/mensajes-error.ts` — no existing `login.tsx` behaviour touched |
| 6a | Shared consumo components (`owner-tabs`, `today-card`, `stock-bar`, `streak-bar`, `empty-state`, `error-retry`) | PR 6a | `pnpm --filter usuario-mobile test` | `pnpm --filter usuario-mobile exec expo export --platform web` — bundle proof the new components compile through Metro | Revert `apps/usuario-mobile/components/consumo/` entirely — unconsumed by any screen until PR6b |
| 6b | `s-consumo` tab: fetch, owner tabs, dose marking, empty/error states | PR 6b | `pnpm --filter usuario-mobile test` | `pnpm --filter usuario-mobile exec expo export --platform web`, manual check against local `core-api` (requires PR3 landed) | Revert `app/(tabs)/consumos.tsx`'s body to `ScreenStub` — screen returns to its pre-change state, no other screen depends on it |
| 7a | `s-consumo-nuevo` create form | PR 7a | `pnpm --filter usuario-mobile test` | `pnpm --filter usuario-mobile exec expo export --platform web` | Revert `app/consumo-nuevo.tsx`'s body to `ScreenStub` |
| 7b | `s-consumo-nuevo-pet` two-step create flow | PR 7b | `pnpm --filter usuario-mobile test` | `pnpm --filter usuario-mobile exec expo export --platform web`, manual check of the 0-pet path against local `core-api` | Revert `app/consumo-nuevo-pet.tsx`'s body to `ScreenStub` |
| 8 | `s-consumo-config` read-only actives list | PR 8 | `pnpm --filter usuario-mobile test` | `pnpm --filter usuario-mobile exec expo export --platform web` | Revert `app/consumo-config.tsx`'s body to `ScreenStub` |
| 9 | `s-consumo-historial` 7-day view | PR 9 | `pnpm --filter usuario-mobile test` | `pnpm --filter usuario-mobile exec expo export --platform web` | Revert `app/consumo-historial.tsx`'s body to `ScreenStub` |
| 10 | Declared `SPEC.md` deltas | PR 10 | N/A — docs only | N/A — no runtime | Revert the 2 edited paragraphs in `apps/usuario-mobile/SPEC.md`, independent of code |

## Phase 1 (PR1): `@repon/types` — consumo view types — D-2, D-5

- [x] 1.1 Added `UserConsumptionListItem`, `AdherenciaEstado`, `AdherenciaDia`, `AdherenciaItem`, `AdherenciaSemanal` to `packages/types/src/consumo.ts`, exact shapes from `design.md` D-2/D-5, with the same rationale comments D-2 states (fractional `esperadas`, `rachaDias` counts from yesterday, no `null` skeleton). No `.spec.ts`/RED step: confirmed via `tasks.md`'s own Key Learning — `packages/types` has no Jest runner in this project, `tsc --noEmit` is its only verification, same as every prior type-only PR in this workspace.
- [x] 1.2 **Verify**: `pnpm --filter @repon/types exec tsc --noEmit` — clean; workspace `pnpm typecheck` (5/5 packages) — clean, no consumer regression; `pnpm lint` — clean.

## Phase 2 (PR2): Backend ports-out + Kysely adapters + domain pure functions — D-2, D-3

- [x] 2.1 Added `findByUserId(userId, tx?): Promise<Pet[]>` to `ports-out/pet-repository.port.ts`.
- [x] 2.2 Implemented `findByUserId` in `adapters/persistence/kysely-pet.repository.ts` reusing `mapPetRow`, `orderBy('created_at', 'asc')`; extended `kysely-pet.repository.spec.ts` (3 new tests: `user_id = $1`+`orderBy` assertion, row mapping, empty array).
- [x] 2.3 Added `findByUserId(userId, tx?): Promise<UserConsumption[]>` to `ports-out/consumption-repository.port.ts`.
- [x] 2.4 Implemented `findByUserId` in `adapters/persistence/kysely-consumption.repository.ts` reusing `mapUserConsumptionRow`; extended `kysely-consumption.repository.spec.ts` (3 new tests, same pattern as 2.2).
- [x] 2.5 Added `ConteoDiarioLog` + `contarTomasPorDia(consumptionIds, desde, hasta, zonaHoraria, tx?)` to `ports-out/consumption-log-repository.port.ts`; appended the "superseded by" docblock note on `adherenciaUltimos7Dias` (left in place, untouched — D-3).
- [x] 2.6 Implemented `contarTomasPorDia` in `adapters/persistence/kysely-consumption-log.repository.ts` — empty-array short-circuit, grouped `to_char((tomado_at at time zone $tz)::date, ...)` query, `at time zone` only in `SELECT`/`GROUP BY`; extended `kysely-consumption-log.repository.spec.ts` (6 new tests: short-circuit, WHERE scoping, no-timezone-in-WHERE, groupBy shape via `toOperationNode()` introspection — `String()` on a Kysely `RawBuilder` is `"[object Object]"`, not the SQL text — row mapping, tx propagation).
- [x] 2.7 Added `dosisEsperadasPorDia`, `ventanaAdherencia` (+ `VentanaAdherencia`), `estadoAdherenciaDia`, `rachaDias` to `domain/consumo.calculos.ts` — all pure, no I/O, no `Date.now()` inside. `ventanaAdherencia`'s local-midnight-to-UTC conversion uses the standard 2-pass `Intl.DateTimeFormat`-based convergence technique (format a guess in the zone, measure its offset, correct, repeat once) — no date library in this workspace, and a fixed-offset shortcut would silently misplace boundary doses around a DST change.
- [x] 2.8 Added `ZONA_HORARIA_ADHERENCIA = 'America/Santiago'` to `domain/consumo.constants.ts`, beside `UMBRAL_STOCK_BAJO_DIAS`.
- [x] 2.9 Extended `domain/consumo.calculos.spec.ts` (14 new tests, table-driven): `dosisEsperadasPorDia` fractional case; `estadoAdherenciaDia`'s 4 thresholds incl. `esperadas===0` checked before `tomadas===0`; `rachaDias` excludes today (an all-`cumplido` 7-day window yields streak 6, not 7) and stops at the first non-`cumplido` day; `ventanaAdherencia` returns exactly 7 dates, correct `desdeUtc`/`hastaUtc` in a no-DST month, and — real DST boundary, not assumed — a window spanning 2026-09-06 (confirmed via Node's actual ICU tzdata: `America/Santiago` jumps GMT-4→GMT-3 at `2026-09-06T04:00:00Z`, local midnight that day doesn't exist) still returns 7 consecutive calendar dates with a correctly-shortened 167h total span, not the naive 168h a fixed-offset bug would produce.
- [x] 2.10 **Verify**: `pnpm --filter core-api exec jest domains/consumo` — 142/142 passed; **full unit suite** — 89/89 suites, 881/881 tests; **full e2e suite** — 27/27 suites, 168/168 tests (one transient failure in `identidad-sesion.e2e-spec.ts` on the first full run — unrelated domain, confirmed the pre-existing non-deterministic e2e flake documented during `mobile-auth-login`'s verify phase, not a regression: passed 15/15 in isolation and the full suite re-ran 168/168 clean); `pnpm lint` — clean; `pnpm typecheck` (workspace-wide) — clean, after fixing 9 pre-existing spec/e2e files whose `jest.Mocked<PetRepository|ConsumptionRepository|ConsumptionLogRepository>` object literals were missing the newly-widened methods (found via the full typecheck, not the scoped test — same class of regression PR4 caught in `mobile-auth-login`).

## Phase 3 (PR3): List endpoints — `mis-mascotas` + `mis-consumos` — D-1, D-4, D-5, D7

- [x] 3.1 Created `ports-in/listar-mascotas.use-case.ts`: `ListarMascotasUseCase.execute(userId) -> PetRepository.findByUserId(userId)`, a thin pass-through.
- [x] 3.2 Created `ports-in/listar-consumos.use-case.ts`: `ListarConsumosUseCase.execute(userId) -> ConsumptionRepository.findByUserId(userId)`, attaches `diasRestantes` per row in-process via `consumoDiario`+`diasRestantes` (D7 — exactly one repository call regardless of row count). Deliberately does NOT guard a degenerate row's non-finite result itself — that's `consumo.mapper.ts`'s job (D-5), kept at the DTO boundary, not the use case.
- [x] 3.3 Tests: `listar-mascotas.use-case.spec.ts` (3 tests) + `listar-consumos.use-case.spec.ts` (4 tests) — `diasRestantes` matches `consumoDiario`+`diasRestantes` on the same input; exactly one repo call regardless of row count (D7); a degenerate row (`horarios: []`) produces a non-finite raw value, confirming the guard genuinely lives downstream, not duplicated here; empty array for a user with nothing.
- [x] 3.4 Created `adapters/http/dto/consumo-list-item-response.dto.ts`: `ConsumoListItemResponseDto extends UserConsumptionResponseDto { diasRestantes: number }`.
- [x] 3.5 Appended `toConsumoListItemResponseDto` to `adapters/http/consumo.mapper.ts` with the `Number.isFinite(dias) ? dias : 0` serialization guard (D-5) — `Infinity`/`NaN` both serialize as JSON `null` otherwise, which the client has no defined handling for.
- [x] 3.6 Added `GET /consumo/mis-mascotas` and `GET /consumo/mis-consumos` to `adapters/http/consumo.controller.ts`, `userId` sourced only from `@Actor() actor.profileId`, Swagger decorators matching the existing routes' style.
- [x] 3.7 Registered `ListarMascotasUseCase` + `ListarConsumosUseCase` in `consumo.module.ts`'s `providers`.
- [x] 3.8 Created `test/consumo-listar.e2e-spec.ts` (9 tests, 2 describe blocks): per endpoint — a cross-tenant world (mocked repo keyed by `userId`) proves only the caller's own rows come back; a `?userId=`/`?petId=` query param changes nothing (asserts the repository mock was called with the actor's id and explicitly NOT the query param's id); empty collection is `200 []`, never `404`; 401 unauthenticated. `GET /consumo/mis-consumos` additionally asserts an exact `diasRestantes` value end-to-end (stock 10 / consumoDiario 4 = 2.5 → floors to 2, matching the real formula) and that a degenerate row serializes `0`, never `null`.
- [x] 3.9 **Verify**: `pnpm --filter core-api exec jest domains/consumo/ports-in/listar` — 7/7 passed; `pnpm --filter core-api exec jest --config ./test/jest-e2e.json consumo-listar` — 9/9 passed; **full unit suite** — 91/91 suites, 888/888 tests; **full e2e suite** — 28/28 suites, 177/177 tests (after a real fix, see below); `pnpm lint` — clean; `pnpm typecheck` — clean.

**A real, expected test failure found and fixed by running the full e2e suite (not the scoped command)**: `test/consumo-cron-no-http-surface.e2e-spec.ts` enumerates `ConsumoController`'s *entire* route table via decorator-metadata reflection, to structurally prove the cron job has no HTTP surface — it asserted an exact count (4) and an exact path list. Adding the 2 new `GET` routes legitimately grew the table to 6 (both new routes share a URL with an existing `POST` at the same path, differing only by HTTP method). Updated the test's expectations to the correct 6-route table, and strengthened the assertion from path-only to `${method} ${path}` pairs — the old assertion would have silently accepted a duplicate-path collision without checking method, which a metadata-reflection route-table test should catch, not gloss over.

## Phase 4 (PR4): Adherence endpoint — `mi-adherencia` — D-1, D-2, D-4, D-5

- [x] 4.1 Created `ports-in/calcular-adherencia-semanal.use-case.ts`: `ventanaAdherencia` → `ConsumptionRepository.findByUserId` (the id set, actor-scoped, D-4) → `ConsumptionLogRepository.contarTomasPorDia` → full aggregation into `AdherenciaSemanal` — per-item per-day `estado` via `estadoAdherenciaDia`, per-item totals, aggregate-across-items `dias`/`porcentaje`, and `rachaDias` over the aggregate day sequence. All aggregation lives here (not the mapper, not the client — D6).
- [x] 4.2 Tests — `calcular-adherencia-semanal.use-case.spec.ts` (8 tests): `dias.length === 7` always; zero consumptions → the full `sin_datos` skeleton (`items: []`, `porcentaje: 0`, `rachaDias: 0`, every day `esperadas===0`); `contarTomasPorDia` called exactly once with exactly the id set `findByUserId` returned; a fully-adhered daily item is 100%/`cumplido` every day with a 6-day streak (today excluded); a day with no `ConteoDiarioLog` row defaults to `tomadas: 0`, not a crash; multiple items aggregate correctly into the top-level `dias`/`porcentaje` (not just the first item); `ahora` defaults to the real clock when omitted.
- [x] 4.3 Created `adapters/http/dto/adherencia-response.dto.ts`: `AdherenciaDiaDto`, `AdherenciaItemDto`, `AdherenciaResponseDto`, `@ApiProperty({ type: [...] })` on `dias`/`items`.
- [x] 4.4 Appended `toAdherenciaResponseDto` to `adapters/http/consumo.mapper.ts` — a thin field-for-field conversion (`@repon/types.AdherenciaSemanal` already matches the DTO shape 1:1); all aggregation logic stays in the use case, never duplicated here.
- [x] 4.5 Added `GET /consumo/mi-adherencia` to `adapters/http/consumo.controller.ts` — 2-segment path, not nested under `mis-consumos/` (D-1: so no future `:consumptionId` route can ever shadow it).
- [x] 4.6 Registered `CalcularAdherenciaSemanalUseCase` in `consumo.module.ts`.
- [x] 4.7 Created `test/consumo-adherencia.e2e-spec.ts` (6 tests). **Corrected the task's own literal wording while implementing** (flagged here rather than silently reinterpreted): "a user with consumptions but no logs gets `items: []`" is self-contradictory — a user *with* consumptions gets a populated `items` array (all `incumplido`, since `tomadas=0` and `esperadas>0` for any real item), never `items: []`. `items: []` is specifically the *zero-consumptions* case. Wrote both as two distinct, correctly-labeled tests instead of one conflated one. Also covers: cross-tenant isolation (mocked repo keyed by `userId`); the log-repository is never queried with a foreign/client-supplied id, only the id set `findByUserId` itself returned (D-4); a log row outside the 7-day window (simulating a misbehaving repository) is not credited, proving the use case trusts only `fechas` it computed, not every row a mock might hand back; 401 unauthenticated.
- [x] 4.8 **Verify**: `pnpm --filter core-api exec jest domains/consumo/ports-in/calcular-adherencia` — 8/8 passed; `pnpm --filter core-api exec jest --config ./test/jest-e2e.json consumo-adherencia` — 6/6 passed; **full unit suite** — 92/92 suites, 896/896 tests; **full e2e suite** — 29/29 suites, 183/183 tests (two transient failures across two full-suite attempts, in `catalogo-ajustes-precio.e2e-spec.ts` then `ofertas-obtener-bandeja.e2e-spec.ts` — both unrelated domains, both clean in isolation, both absent on a third full run; same pre-existing non-deterministic e2e flake class documented repeatedly this session, not a regression); `pnpm lint` — clean; `pnpm typecheck` — clean (after updating `consumo-cron-no-http-surface.e2e-spec.ts`'s route table to 7 routes, same fix pattern as PR3, expected and applied proactively this time).

## Phase 5 (PR5): `@repon/auth` JSON/error helper + Spanish mapping — D-10

- [x] 5.1 Created `packages/auth/src/api-json.ts`: `ApiError`, `NetworkError` (`code` fixed to `'RED_NO_DISPONIBLE'`); `getJson`/`postJson`/`postNoContent` via a shared `callAuthFetch` wrapper — sets `Content-Type`+`JSON.stringify`s on POST, `!response.ok` throws `ApiError(status, code, message)` per the `session-client.ts` envelope shape (identical `.catch(() => null)` fallback), `postNoContent` asserts `204` and never calls `.json()`. `callAuthFetch` distinguishes `authFetch`'s own `'authFetch called with no active session'` (rethrown untouched — a programming error, not a network state) from every other rejection (wrapped as `NetworkError`) — the one place this PR had to design beyond design.md's pinned behaviour table, since both cases arrive as a rejected `authFetch(...)` call with no other built-in signal to tell them apart.
- [x] 5.2 Tests — `packages/auth/src/api-json.spec.ts` (14 tests): `Content-Type`+stringified body set on POST; GET sends no body; `!ok` throws `ApiError` with the parsed `code` (both `getJson`/`postJson`/`postNoContent`); an unparseable error body falls back to `UNKNOWN_ERROR`/`'Unexpected error'`; `postNoContent` never calls `.json()` on success (spied directly on the response); a rejected `authFetch` call becomes `NetworkError`; `authFetch`'s own `'authFetch called with no active session'` error propagates untouched, confirmed NOT an instance of `NetworkError`; `NetworkError.code` is always `'RED_NO_DISPONIBLE'`.
- [x] 5.3 Modified `packages/auth/src/index.ts`: barrel-exports `ApiError`, `NetworkError`, `getJson`, `postJson`, `postNoContent`, `AuthFetch`.
- [x] 5.4 Created `apps/usuario-mobile/lib/mensajes-error.ts`: `MENSAJES` record (the 5 `ConsumoExceptionFilter` codes + `RED_NO_DISPONIBLE`), `mensajeDeError()` reading `.code` off `ApiError | NetworkError | SesionApiError` via an `instanceof` union check, `POR_DEFECTO` fallback for any other value (including a plain `Error`, a string, or `null`).
- [x] 5.5 Tests — `apps/usuario-mobile/lib/__tests__/mensajes-error.test.ts` (6 tests): all 6 known codes (5 `ConsumoExceptionFilter` codes + network) map to distinct, non-empty strings; an unknown `ApiError` code and an unknown `SesionApiError` code both fall to `POR_DEFECTO`; `NetworkError` maps correctly; a plain `Error`/string/`null` all fall to `POR_DEFECTO` too (not just "unknown code" — "not a wire-error type at all").
- [x] 5.6 **Verify**: `pnpm --filter @repon/auth test` — 49/49 passed (35 pre-existing + 14 new); `pnpm --filter usuario-mobile test` — 17/17 passed (11 pre-existing + 6 new); `pnpm --filter proveedor-mobile test` re-run — 13/13 unaffected; **core-api full unit suite** re-run — 92/92 suites, 896/896 tests unaffected; workspace `pnpm typecheck` — clean (5/5 packages); `pnpm lint` (workspace) — clean after removing one unused import the scoped test file's first draft left in.

## Phase 6 (PR6a): Shared consumo UI components — D-9, presentational pieces of D-8

- [x] 6.1 Created `components/consumo/owner-tabs.tsx`: `OwnerTab` type (`{key:'self'} | {key:'pet', petId}`) + `ownerTabKey` helper + `<OwnerTabs>` — `null` at 0 pets, `[Yo][<pet>]` at 1, `ScrollView horizontal` strip at N (D-9). Filtering/selection stay the caller's job (screen-local `useState`, per D-9's own note) — this component only renders the strip and reports taps via `onSelect`.
- [x] 6.2 Created `components/consumo/today-card.tsx`: per-item card (nombre, dosis/horarios, embeds `<StockBar>`, a mark-dose button). Renders `diasRestantes`/`stockActual` exactly as received via `StockBar`, no client recomputation. `marcado` is an explicit prop, documented as session-local (no "marked today" field exists anywhere in `@repon/types`/the wire shapes) — the caller (PR6b) is responsible for only flipping it true after a `postNoContent` + refetch round-trip succeeds, never optimistically.
- [x] 6.3 Created `components/consumo/stock-bar.tsx`: `unidad === 'g'` and value `>= 1000` (inclusive) renders kilograms with one decimal; every other unit/value renders as-is. The API never sees the kilogram conversion — display-only.
- [x] 6.4 Created `components/consumo/streak-bar.tsx`: `rachaDias` + a 7-cell day strip coloured from the 4-value `estado` enum via a fixed lookup table — no `tomadas`/`esperadas` comparison anywhere in this component (D-2/D6).
- [x] 6.5 Created `components/consumo/empty-state.tsx`: `titulo`/`descripcion`/`acciones[]` (0-2 CTAs), reused for the D-8 first-run block and every per-owner empty state.
- [x] 6.6 Created `components/consumo/error-retry.tsx`: Spanish `mensaje` + a "Reintentar" button, distinct red-tinted styling from `EmptyState`.
- [x] 6.7 Tests — `owner-tabs.test.tsx` (4), `stock-bar.test.tsx` (6), `streak-bar.test.tsx` (4) — RNTL, props-driven only, no live fetch. One real bug found and fixed in the *test*, not the component: `Themed.View` wraps its own `style` prop inside `[{backgroundColor: theme}, style]`, so a shallow `.find()` for `backgroundColor` in `streak-bar.test.tsx` matched the theme's own background before ever reaching the per-`estado` colour — fixed by fully flattening and merging every style object in array order (mirroring how RN itself resolves a style array), not just taking the first match.
- [x] 6.8 **Verify**: `pnpm --filter usuario-mobile test` — 31/31 passed (17 pre-existing + 14 new across the 3 new test files); real Metro bundle (`expo export --platform web`) — succeeded, 23 static routes (unchanged from PR9/PR10, confirming these new components don't break the existing bundle); `pnpm lint` (workspace) — clean; `pnpm typecheck` (workspace) — clean.

## Phase 7 (PR6b): `s-consumo` tab — fetch, dose marking, empty/error states — D-7, D-8, D-9

- [x] 7.1 Created `app/(tabs)/consumos.tsx`: `version`-counter effect, `Promise.all(getJson('/consumo/mis-mascotas'), getJson('/consumo/mis-consumos'))`, `useState` results, `cancelled` cleanup guard — exact skeleton from design.md D-7's own code block, typed against `@repon/types.Pet`/`UserConsumptionListItem` (the design snippet's `PetResponseDto` name was pseudocode for the same wire shape, not a real import from `core-api`).
- [x] 7.2 Same file: `marcar(id)` → `postNoContent('.../dosis')` → on success adds `id` to a session-local `marcadosHoy: Set<string>` (documented: no field anywhere in `@repon/types` records "marked today" — this is the only honest source) then `setVersion(v+1)`; `errorDosis[id]` on failure; `marcando`/disabled state. No optimistic flip — `marcadosHoy` is updated only inside the `try` block after `postNoContent` resolves, never on tap.
- [x] 7.3 Same file: **5** distinct states per D-8's table (the task text says "four" but design.md's own table lists 5 rows — implemented all 5, flagged here as another minor task-text/design-doc count mismatch, same class as PR4's task 4.7): loading (`ActivityIndicator`); load-failed (`ErrorRetry` + "Reintentar" bumping `version`); 0-pets-and-0-consumptions global first-run block (2 CTAs, no owner tabs — achieved for free since `<OwnerTabs>` already returns `null` at 0 pets, PR6a); 0-pets-with-self-consumptions (tabs still auto-hidden, self list renders directly, inline "Agregar una mascota" link appended below); ≥1-pet owner tabs with a per-owner `EmptyState` when the selected tab's filtered list is empty. `s-consumo-config`/`s-consumo-historial` nav links render above every branch (including loading/error), never conditional.
- [x] 7.4 Same file: local filtering by `OwnerTab` over the single `mis-consumos` response (`self` → `ownerType === 'self'`; a pet tab → `petId === tab.petId`) — confirmed by a dedicated test asserting the fetch call count doesn't change when switching tabs (no per-owner endpoint, no `?ownerType=` param, R5/D-4).
- [x] 7.5 Created `app/(tabs)/__tests__/consumos.test.tsx` (9 tests, all passed on the first run — both of PR9/PR10's RNTL lessons, awaited `fireEvent`/`render` and rehydration-wait, applied proactively): loading → loaded; zero-state renders both CTAs and no owner tabs; a load failure renders `ErrorRetry`, never `EmptyState`; Reintentar recovers into the loaded list; N pets render N+1 tabs; local filtering by tab with an exact fetch-call-count assertion; mark-dose posts once, triggers exactly 2 refetch GETs, and the checkmark flips only after those resolve; a failed mark shows the exact Spanish message from `mensajeDeError` and a retry succeeds; nav links are reachable while loading.
- [x] 7.6 **Verify**: `pnpm --filter usuario-mobile test` — 40/40 passed (31 pre-existing + 9 new); real Metro bundle (`expo export --platform web`) — succeeded, 23 static routes unchanged; `pnpm --filter proveedor-mobile test` re-run — 13/13 unaffected; core-api full unit suite re-run — 92/92 suites, 896/896 tests unaffected; workspace `pnpm typecheck` — clean; workspace `pnpm lint` — clean (one invalid `eslint-disable-next-line react-hooks/exhaustive-deps` comment removed — this project's ESLint config doesn't register that rule at all, and the effect didn't actually need suppressing).

## Phase 8 (PR7a): `s-consumo-nuevo` — self create form — D-5, D-6

- [x] 8.1 Created `app/consumo-nuevo.tsx`: `medicamento`/`suplemento` fields per D-6's shared block (D5); `ownerType: 'self'`, no `petId`. **Scope correction, documented, not silently applied**: the task text implies this screen needs `alimento`/`vacuna` handling too, but `s-consumo-nuevo`'s own mockup (`mockups/usuario.html:597-680`) is a single flat form with no 4-way kind picker — unlike `s-consumo-nuevo-pet`'s explicit `selectPetType`. `alimento`/`vacuna` are pet-only concepts in this app's data model (a human doesn't log "alimento"/pet vaccines against themselves); they belong exclusively to PR7b's pet form. This screen offers a 2-way `medicamento`/`suplemento` chip selector instead.
- [x] 8.2 Same file: submit → `postJson('/consumo/mis-consumos', ...)`; `201` → `router.back()`; non-2xx → inline `mensajeDeError(e)`, form retained (no `router.back()` call on failure).
- [x] 8.3 Same file: only the 8 real `NuevoConsumoDto` fields this kind pair needs are ever collected — none of the 7 dropped mockup-only fields (weekday picker, context select, 2 dead toggles, última aplicación, duración) have any UI at all, so there's nothing to accidentally serialize.
- [x] 8.4 Created `app/__tests__/consumo-nuevo.test.tsx` (6 tests, all passed on the first run): default submission is an exact, valid `medicamento` payload (`ownerType:'self'`, no `petId`); switching the kind chip changes only `kind`; "veces al día" resizes the `horarios` array and the resized value round-trips into the payload; `201` calls `router.back()`; a `400 CONSUMO_INVALIDO` shows the exact mapped Spanish message and does NOT call `back()`; the payload's key set is exactly the 9 `NuevoConsumoDto` fields this screen sends (`ownerType`+8), confirmed no dropped-field key ever appears.
- [x] 8.5 **Verify**: `pnpm --filter usuario-mobile test` — 46/46 passed (40 pre-existing + 6 new); real Metro bundle — succeeded, 23 routes unchanged; workspace `pnpm typecheck` — clean; workspace `pnpm lint` — clean.

## Phase 9 (PR7b): `s-consumo-nuevo-pet` — two-step create flow — D-6, D-8

- [x] 9.1 Created `app/consumo-nuevo-pet.tsx`: 0-pets path — pet form shown directly (no selector) → `postJson('/consumo/mis-mascotas', {...})` → `201 {id}` sets `mascotaId`, advancing to step 2 with `petId` pre-filled → step-2 consumo form → `postJson('/consumo/mis-consumos', ...)`.
- [x] 9.2 Same file: ≥1-pet path — a pet selector (tap any pet → skip straight to step 2) + a "+ nueva mascota" link that reveals the same pet-creation form inline, without posting anything until its own submit.
- [x] 9.3 Same file: partial-failure rule — `mascotaId` state is the ONLY gate between step 1 and step 2; once non-null there is no code path back to the pet-creation form for the rest of this screen's lifetime, so a step-2 failure/retry structurally cannot re-POST the pet (not just "doesn't happen to" — there is no button/effect left that could).
- [x] 9.4 Created `app/__tests__/consumo-nuevo-pet.test.tsx` (9 tests, all passed first run): 0-pets shows the form directly, no selector; step-1 success pre-fills `petId` into a valid step-2 payload; ≥1-pet shows a selector never the create form; selecting an existing pet skips to step 2; "+ nueva mascota" reveals the form without posting; `alimento`'s kg→g conversion (`stockKg=15` → `stockActual:15000`, `dosisPorToma` sent as-is); `vacuna` sends a real `horarios`/`stockActual`, confirmed absent of `ultimaAplicacion`/`duracion`; `suplemento` reuses the medicamento block; the partial-failure sequence (step 1 succeeds, step 2 fails once, retry succeeds) asserts the pet-creation `POST` fired exactly once total across both consumo attempts.
- [x] 9.5 **Verify**: `pnpm --filter usuario-mobile test` — 55/55 passed (46 pre-existing + 9 new); real Metro bundle — succeeded, 23 routes unchanged; workspace `pnpm typecheck` — clean; workspace `pnpm lint` — clean.
- [x] 9.6 **Post-verify fix (found by `sdd-verify`, CRITICAL)**: the initial `GET /consumo/mis-mascotas` fetch mapped ANY failure to `setMascotas([])`, silently conflating a backend outage with a legitimate 0-pets first-run user — no `ErrorRetry`, no error message, no manual retry, and zero test coverage of the failure path (violated spec Requirement 7 and D-8's own anti-conflation discipline, which the other 3 read screens already honoured correctly). Fixed: failures now set a distinct `errorCargaMascotas` state, rendered via the shared `<ErrorRetry>` (same component/pattern as `consumos.tsx`/`consumo-config.tsx`/`consumo-historial.tsx`), with a `versionMascotas` counter driving retry — consistent with every sibling screen's error-handling shape. 2 new tests added to `consumo-nuevo-pet.test.tsx` (RED confirmed before the fix, GREEN after): failure renders `ErrorRetry` never the 0-pets form; retry re-fetches and recovers. Full gate re-run: `pnpm --filter usuario-mobile test` 66/66 (64 prior, after PR9's consumo-historial, + 2 new), workspace `pnpm typecheck`/`pnpm lint` clean, real Metro export succeeded (23 routes unchanged).

## Phase 10 (PR8): `s-consumo-config` — read-only actives list — D-4, D-9

- [x] 10.1 Created `app/consumo-config.tsx`: read-only actives list sourced from the same `mis-mascotas`/`mis-consumos` reads as the tab, `<OwnerTabs>` reused, `<StockBar>` per item.
- [x] 10.2 Same file: "editar" is a `disabled` `Pressable` with visible copy ("Editar (edición disponible próximamente)") — no `onPress` navigation logic exists on it at all, so there's no code path to a create form even to accidentally trigger.
- [x] 10.3 Created `app/__tests__/consumo-config.test.tsx` (5 tests, all passed first run): active items render with stock/days; "editar" is confirmed disabled via `accessibilityState`; a full test suite run's entire `fetchSpy` call log is scanned to confirm zero `POST` methods ever fired after pressing "editar" (the exact spec scenario, not just "no navigation happened"); a distinct `EmptyState` when nothing is active; a distinct `ErrorRetry` on load failure.
- [x] 10.4 **Verify**: `pnpm --filter usuario-mobile test` — 60/60 passed (55 pre-existing + 5 new); workspace `pnpm typecheck` — clean; workspace `pnpm lint` — clean. (No Metro-bundle gate named for this task, unlike most others — task 10.4's own wording omits it.)

## Phase 11 (PR9): `s-consumo-historial` — 7-day adherence view — D-2, D-9

- [x] 11.1 RED->GREEN `app/consumo-historial.tsx`: fetch `GET /consumo/mi-adherencia` on its own `version` counter (separate from the tab's — D-1 scoping), `<OwnerTabs>` reused for local presentation grouping. **Resolved gap**: also fetches `GET /consumo/mis-mascotas` — `AdherenciaItem.nombre` is the *consumption's* name, not the pet's, and D-9 pins owner-tab labels to `pet.nombre` for all three owner-tabbed screens; `AdherenciaSemanal` alone cannot supply it. Documented in `consumo-historial.tsx`'s file comment, not silently applied.
- [x] 11.2 RED->GREEN same file: 7-day grid per item (`dias[7]`, `estado` -> colour, no client math), per-item `porcentaje`, aggregate `porcentaje`+`rachaDias` header (via `<StreakBar>`), a "desde-hasta" window label (`DD/MM – DD/MM`, string-split on the already-local `YYYY-MM-DD`, no `Date`/timezone involved) replacing the mockup's month header (D-2, "no month view" rule). Aggregate header always reflects the full server response, never recomputed per owner tab — only the per-item list is locally filtered (D6: no client aggregation math).
- [x] 11.3 Tests — new `app/__tests__/consumo-historial.test.tsx`: figures rendered equal the server's values with no local recomputation (spec scenario); the `sin_datos` skeleton renders without a null/crash for a zero-activity user (`items: []`, 7 `sin_datos` days); loading/empty/error states distinct. 4/4 passing.
- [x] 11.4 **Verify**: `pnpm --filter usuario-mobile test` (64/64: 60 pre-existing + 4 new); `pnpm --filter usuario-mobile exec expo export --platform web` (`/consumo-historial` bundled, 23 static routes); `pnpm lint` (clean); `pnpm typecheck` (workspace-wide, "Scope: 5 of 6 workspace projects", clean).

## Phase 12 (PR10): Declared `SPEC.md` deltas — D2, D4

- [x] 12.1 Modify `apps/usuario-mobile/SPEC.md`'s "Estado que maneja el cliente" section: state that Consumos specifically uses `authFetch` + local `useState`/`StyleSheet` (this change's precedent), not Zustand/TanStack Query — scoped to the consumo domain only, other entries in that section unchanged (D2 declared delta). Added a new bullet naming all 5 consumo screens; the 3 pre-existing bullets (refill/Zustand, tab-selection/local, catálogo-ofertas-historial/TanStack) left untouched.
- [x] 12.2 Modify `apps/usuario-mobile/SPEC.md`'s "Pendiente al migrar" line ("mutaciones reales contra Supabase"): correct to real mutations against `core-api`'s consumo routes, never Supabase directly; add a short note recording D4's create-only/no-reconfigure scope for `s-consumo-config`. `aceptarOferta` kept in the still-pending list (unrelated domain, unresolved); `marcarDosis` moved to resolved.
- [x] 12.3 **Verify**: manual proofread of both edits against PR1-PR9's shipped route paths/screen behaviour — confirmed against `consumo.controller.ts`'s live `@Get`/`@Post` decorators: `mis-mascotas` (GET+POST), `mis-consumos` (GET+POST), `mi-adherencia` (GET), `mis-consumos/:id/dias-restantes` (GET), `mis-consumos/:id/dosis` (POST) — no reconfigure/edit route exists, confirming the "solo lectura" claim. No code touched, no test command applicable. Note: `openspec/specs/core-api-consumo/spec.md`'s base-capability merge is owned by `sdd-archive` (the delta already exists at `openspec/changes/usuario-mobile-consumo/specs/core-api-consumo/spec.md`) — not a manual task here.
