# Tasks: `core-api` — fundación ejecutable (NestJS hexagonal + toolchain del monorepo)

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~3,300-3,800 total across 9 chained PRs; per-PR range 180-700 |
| 400-line budget risk | High overall — PR5 (kernel/auth), PR6 (4a), PR7 (4b), PR8 (4c) each individually risk exceeding 400 |
| Chained PRs recommended | Yes — 9 sequential PRs, dependency-ordered (boot needs types, kernel needs boot, identidad needs kernel), cannot parallelize |
| Suggested split | PR1 (0 toolchain) → PR2 (1 types) → PR3 (2 boot) → PR4 (3.1-3.5 kernel infra) → PR5 (3.6-3.9 kernel auth) → PR6 (4a identidad core) → PR7 (4b use cases) → PR8 (4c HTTP adapter) → PR9 (5 closure) |
| Delivery strategy | auto-chain (cached this session) |
| Chain strategy | stacked-to-main (consistent with `backend-supabase-migrations` precedent — each PR merges to `main` in dependency order, no unmerged parent branches) |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

### Per-PR estimate

| PR | Slice | Est. lines | Risk | Note |
|---|---|---|---|---|
| 1 | 0 toolchain | 200-250 | Low | Config only |
| 2 | 1 `@repon/types` | 250-300 | Low-Medium | Mechanical, SPEC.md ts block is ~185 lines |
| 3 | 2 boot | 280-320 | Medium | First runtime code, Node 24/Nest 11 compat check (R7) |
| 4 | 3.1-3.5 kernel infra (db/supabase/event-bus/audit) | 300-350 | Medium | Includes opt-in DB-role integration test (D-A risk) |
| 5 | 3.6-3.9 kernel auth (guards/verifiers/decorators) + notif/payment stubs | 450-550 | **High** | 11-branch guard matrix is non-negotiable (D7) — do not trim tests to fit budget; use `size:exception` if it can't shrink |
| 6 | 4a identidad core + guard activation | 450-550 | **High** | Domain + 4 ports-out + 3 Kysely repos + actor adapter + `APP_GUARD` registration |
| 7 | 4b use cases + tests | 600-750 | **High** | Largest PR. Fallback split if over budget: 7a `RegistrarUsuarioUseCase` (saga, D7's most critical test) / 7b other 5 use cases + tests |
| 8 | 4c HTTP adapter | 450-500 | Medium-High | Mechanical (DTO/controller/mapper repeat pattern) — faster review despite line count |
| 9 | 5 closure | 150-200 | Low | 5 placeholders + config flip |

### Suggested Work Units

| Unit | Goal | PR | Base | Notes |
|---|---|---|---|---|
| 1 | Toolchain | PR 1 | `main` | No dependency; blocks all others |
| 2 | `@repon/types` | PR 2 | `main` | Depends on PR 1 |
| 3 | Boot | PR 3 | `main` | Depends on PR 2 (`@repon/types` importable) |
| 4 | Kernel infra | PR 4 | `main` | Depends on PR 3 |
| 5 | Kernel auth | PR 5 | `main` | Depends on PR 4; watch budget, D7 tests non-negotiable |
| 6 | Identidad 4a | PR 6 | `main` | Depends on PR 5; moves `APP_GUARD` here per design.md §7 |
| 7 | Identidad 4b | PR 7 | `main` | Depends on PR 6; likely needs the 7a/7b fallback split above |
| 8 | Identidad 4c | PR 8 | `main` | Depends on PR 7 |
| 9 | Closure | PR 9 | `main` | Depends on PR 8; flips `strict_tdd: true` — LAST task of the change |

---

## Phase 0: Toolchain — Spec: `repo-toolchain`

- [x] 0.1 Root `package.json` (pnpm workspaces `apps/*`,`services/*`,`packages/*`) + `pnpm-workspace.yaml`.
- [x] 0.2 `tsconfig.base.json` (`strict: true`) at root; every package's `tsconfig.json` will `extend` it, never override `strict`.
- [x] 0.3 `.nvmrc` (Node 24.x) + append `nodejs 24.x` line to `.tool-versions` (D4) — both resolve the same major.
- [x] 0.4 ESLint config incl. `no-restricted-imports`/`import/no-restricted-paths` blocking cross-domain `ports-out/`, `adapters/persistence/`, `adapters/events/`, `domain/` imports (`core-api-hexagonal-layout`); Prettier config; both fail CI on violation.
- [x] 0.5 `.env.example` covering every var from `core-api-bootstrap`'s env schema (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`, `AUTH_JWT_MODE`, `SUPABASE_JWT_SECRET`, `SUPABASE_JWKS_URL`, `AUTH_JWT_ISSUER`, `AUTH_JWT_AUDIENCE`).
- [x] 0.6 `.github/workflows/ci.yml`: install → lint → typecheck → test → build, each step gates the next.
- [x] 0.7 Confirm `openspec/config.yaml` stays `strict_tdd: false` (D7 — flip is Phase 5's last task, not a precondition).

## Phase 1: `@repon/types` — Spec: `shared-types-package`

- [x] 1.1 `packages/types/package.json` + `tsconfig.json` extending base.
- [x] 1.2 `packages/types/src/*.ts`: promote every type/interface from `SPEC.md`'s ts block verbatim (camelCase), grouped by domain file + `index.ts` barrel export. Zero `schema.ts` row types re-exported (D-A boundary).
- [x] 1.3 `packages/types/SPEC.md`: reframe intro as documentation of the code, not the executable source.
- [x] 1.4 Verify `tsc --noEmit` in CI is the check (no dedicated unit tests needed — pure type declarations, no runtime logic).

## Phase 2: Boot — Spec: `core-api-bootstrap`

- [x] 2.1 `services/core-api/package.json`; install NestJS 11, `class-validator`/`class-transformer`, `@nestjs/swagger`, `jose`. (Resolved: `@nestjs/*@11.1.28`, `@nestjs/config@4.0.4`, `@nestjs/swagger@11.4.6`; also added `@nestjs/config` + `zod` per design.md D-D's env schema, not itemized in the original task text.)
- [x] 2.2 `src/main.ts`: global `ValidationPipe` (whitelist+forbidNonWhitelisted), global exception filter skeleton, Swagger mount gated on `NODE_ENV !== 'production'` (D5). (Global exception filter skeleton deferred to PR 5/`shared/auth` per design.md D-E — "a global exception filter → `{statusCode, code, message}`" is scoped there, nothing in this PR throws a domain exception yet to justify one; ValidationPipe + Swagger gating are real and verified booted.)
- [x] 2.3 `src/config/env.schema.ts`: fail-fast — `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`DATABASE_URL` unconditional; `AUTH_JWT_MODE` discriminated union (`hs256`⇒`SUPABASE_JWT_SECRET`, `jwks`⇒`SUPABASE_JWKS_URL`); `AUTH_JWT_ISSUER`/`AUTH_JWT_AUDIENCE` always required; exits non-zero pre-listen. (Zod `discriminatedUnion`, chosen over Joi for first-class TS inference; verified non-zero exit via a real boot with `SUPABASE_SERVICE_ROLE_KEY` unset — Nest's own default `abortOnError` triggers the exit, not a custom handler; see env.schema.ts's doc comment.)
- [x] 2.4 `src/app.module.ts` (minimal: config module + health only — kernel doesn't exist yet); `src/health/health.controller.ts` — `@Public() GET /health` → 200. (`@Public()` deliberately NOT added: it doesn't exist until PR 5/`shared/auth`, and there is no global guard yet in this PR for it to bypass — the spec's "Health check is unauthenticated" requirement is satisfied because the route is unguarded by construction. Adding a stub decorator now would preempt PR 5's design.)
- [x] 2.5 `jest.config.ts` + `test/jest-e2e.json`. (Used `package.json#jest` instead of a standalone `jest.config.ts` — the actual official NestJS CLI default, avoids an extra `ts-node` dependency just to load a `.ts` config file; `test/jest-e2e.json` is exactly as specified.)
- [x] 2.6 `test/health.e2e-spec.ts`: `GET /health` → 200 unauthenticated. This is the regression detector for Phase 4a's guard activation (design.md §7). (Verified two ways: `Test.createTestingModule` + supertest in CI, AND a real `tsx src/main.ts` process hit with `curl` — both return `{"status":"ok"}` / 200.)
- [x] 2.7 Unit tests: env schema rejects missing `SUPABASE_SERVICE_ROLE_KEY`; rejects `hs256` mode without `SUPABASE_JWT_SECRET`. (9 unit tests total, incl. jwks-without-URL, missing-mode, and out-of-union-mode cases beyond the two the task text names.)

## Phase 3: Kernel — Spec: `core-api-bootstrap`, `shared-audit-log`, `core-api-auth-guard` (design.md order 3.1-3.6)

- [x] 3.1 `shared/database/`: `pool.provider.ts` (pg pool authenticated under `service_role` grants), `schema.ts` (Kysely row types, `snake_case`, never in `@repon/types`), `database.module.ts` (`DATABASE` token), `transaction.ts` (opaque `TransactionContext`, `TransactionManager.runInTransaction`, `TRANSACTION_MANAGER` token). `schema.ts` types only `companies`/`profiles`/`admin_roles`/`audit_log` — this change's scope, not all 17 tables (remaining tables land with their owning domain change). `kysely` pinned to `^0.28.17`, not the newer `^0.29.x` latest: 0.29 dropped its CJS build entirely (ESM-only `export *`), which `ts-jest`'s default CJS transform (and this service's `"type": "commonjs"`) can't consume without a much larger module-system migration — 0.28.17 is the last release shipping both.
- [x] 3.2 Integration test (opt-in via local `supabase start`, excluded from CI — `test:integration` script, not wired into `test`/CI): asserts `UPDATE audit_log` **and** `DELETE audit_log` are rejected under this connection's role, plus `current_user = 'service_role' ≠ session_user`, an adapter-level insert, and `TransactionManager` rollback — all run against a real local Supabase/Postgres instance (`supabase start`, verified green). Closes the open question: `service_role` itself has `rolcanlogin = false` (verified via `\du`/`pg_authid` — it's a grant-holding role, not a login role, by Supabase's own design), so the resolved mechanism is `authenticator` (the same login role PostgREST itself connects as, `NOINHERIT`-granted membership in `service_role`) + an explicit `SET ROLE service_role` on every new physical connection (`pool.provider.ts`), not a brand-new dedicated login role — see `.env.example` for the production-vs-local split this implies.
- [x] 3.3 `shared/supabase/`: `supabase.module.ts` + `SUPABASE_CLIENT` token (Auth Admin + Storage only — no PostgREST business queries; enforced by convention/comment, not the type system — `supabase-js` doesn't expose a narrowed client type).
- [x] 3.4 `shared/event-bus/`: `EventBusModule` (`@nestjs/event-emitter`'s `EventEmitterModule.forRoot()`, wrapping `EventEmitter2`), `DomainEvent` base type, `EventPublisher` port + `EVENT_PUBLISHER` token. `publish()` is `async`/`emitAsync` (not the sync `emit`), so a future `await`ing caller genuinely waits on async listeners once a domain adapter does I/O in one (Phase 4c).
- [x] 3.5 `shared/audit/`: `AuditLogPort`/`AUDIT_LOG_PORT` (write-only `record`, `cambios: {campo:{antes,despues}}` shape fixed) + Kysely-backed adapter + `audit.module.ts` + unit test asserting `tx` pass-through (3 unit tests: default-db path, tx path, motivo passthrough). `cambios` values pass through to `pg` as plain JS objects for the `jsonb` column — no manual `JSON.stringify`, `pg`'s own `prepareValue` serializes objects automatically (verified round-trip against the real DB in the integration test).

**Deviation from task 3.10** (not this PR's scope — orchestrator-scoped to 3.1-3.5 only): `DatabaseModule`/`SupabaseModule`/`EventBusModule`/`AuditModule` are each `@Global()` and imported directly by `app.module.ts` for now, rather than through a `SharedKernelModule` wrapper — task 3.10 literally can't "aggregate 3.1-3.9's providers" until 3.6-3.9 exist (PR 5). PR 5 should introduce `shared-kernel.module.ts` importing these 4 plus its own new modules, then swap `app.module.ts`'s 4 direct imports for the one `SharedKernelModule` import — a mechanical follow-up, not a redesign.
- [x] 3.6 `shared/auth/`: `ActorPort`/`ACTOR_PORT` + `AuthenticatedActor` (interface only, zero domain imports); `JwtVerifier` port + `Hs256`/`Jwks` verifiers via `jose`, selected once at boot by `AUTH_JWT_MODE`.
- [x] 3.7 `shared/auth/`: `AuthGuard` + `RolesGuard` + `@Public()`/`@Roles()`/`@AdminRoles()`/`@Actor()` decorators; `AuthModule` exports guard classes + `JWT_VERIFIER`, does **NOT** register `APP_GUARD` (design.md §7 — no `ACTOR_PORT` provider until Phase 4a); global exception filter → `{statusCode, code, message}`, no stack-trace leakage.
- [x] 3.8 Unit tests: full 11-branch `AuthGuard`/`RolesGuard` matrix (`@Public()` bypass, missing/malformed token, invalid signature/UUID, actor-backend throw→503, null→401, suspended→403, provider+suspended-company→pass, valid→next, no-metadata/role-mismatch/admin-missing/admin-not-allowed/allowed) — `ActorPort`+`JwtVerifier` mocked, no DB. Also added dedicated `Hs256JwtVerifier`/`JwksJwtVerifier`/`selectJwtVerifier` unit tests (real `jose` crypto, not mocked) — `auth.guard.spec.ts` mocks `JwtVerifier` at the port boundary, so the signature/issuer/audience/expiry/algorithm-pinning logic inside the two verifiers was otherwise untested.
- [x] 3.9 `shared/notifications/` + `shared/payments/`: interface + token only (`NOTIFICATION_PORT`, `PAYMENT_GATEWAY_PORT`), no `useValue: {}`, no implementation.
- [x] 3.10 `app.module.ts`: import `SharedKernelModule` (`@Global()`) aggregating 3.1-3.9's providers.

## Phase 4a: Identidad — domain, persistence, actor, guard activation — Spec: `core-api-identidad`, `core-api-auth-guard`, `core-api-hexagonal-layout`

- [x] 4a.1 `domains/identidad/domain/`: `Profile`/`Company`/`AdminRoleAssignment` entities/factories; invariant `role==='provider' ⇒ companyId != null`. (`createProfile`/`createCompany` factories; `AdminRoleAssignment` is a plain interface with no invariant, per corrected spec's disambiguation from the `AdminRole` sub-role enum.)
- [x] 4a.2 `domains/identidad/ports-out/*.port.ts`: `ProfileRepository` (`insertIfAbsent`+`update` split, corrected per spec), `CompanyRepository` (`save`), `AdminRoleRepository` (`upsert`+`findByProfileId`), `AuthProviderPort` (+`AuthProviderDeterministicError`/`AuthProviderAmbiguousError`) — every method carries trailing `tx?: TransactionContext` except `AuthProvider`/`EventPublisher`. (`AuthProviderPort` interface declared, deliberately unbound — no provider until Phase 4b's `SupabaseAuthProvider`.)
- [x] 4a.3 `adapters/persistence/kysely-*.repository.ts` for the 3 Kysely-backed ports. (`KyselyProfileRepository`/`KyselyCompanyRepository`/`KyselyAdminRoleRepository`, each with a `private executor(tx?)` helper selecting the shared `db` vs. the caller's unwrapped `tx`.)
- [x] 4a.4 `domains/identidad/contracts/identidad-actor.adapter.ts`: `IdentidadActorAdapter implements ActorPort`, single JOIN `profiles⋈admin_roles⋈companies`.
- [x] 4a.5 `identidad.module.ts`: bind the 3 repos + `{provide: ACTOR_PORT, useClass: IdentidadActorAdapter}`, `exports: [ACTOR_PORT]`.
- [x] 4a.6 `app.module.ts`: import `IdentidadModule`, register `AuthGuard`+`RolesGuard` as `APP_GUARD` — the ordering constraint design.md §7 fixes (only safe once `ACTOR_PORT` has a provider). (Verified live: real app boots with `IdentidadModule` resolved, `GET /health` → 200, unmapped routes → 404 pre-guard as expected.)
- [x] 4a.7 Unit tests: domain invariant rejection (`profile.entity.spec.ts`); `IdentidadActorAdapter` resolves with one query (`identidad-actor.adapter.spec.ts`, mocked Kysely chain). E2e: protected route → 401 `MISSING_BEARER_TOKEN` no token (`auth-guard-activation.e2e-spec.ts`, via a testing-module-only `ProbeController` — Phase 4c ships the first real protected route); `/health` still 200 (regression check from 2.6). Also added (beyond this task's literal scope, opt-in, excluded from CI): `test/identidad-actor.integration-spec.ts` — `IdentidadActorAdapter` resolves the real seeded `super_admin` row against local Supabase, proving the JOIN's actual SQL against `service_role`'s grants, not just a mocked chain.

## Phase 4b: Identidad — use cases + tests — Spec: `core-api-identidad`, `auth-provisioning`

- [x] 4b.1 `RegistrarUsuarioUseCase`: 3-step saga (`createAccount`→`insertIfAbsent`→`publish`), owning all compensation itself (`auth-provisioning` D-B — never the adapter). (`assertProviderHasCompany` extracted from `createProfile` — `domain/profile.entity.ts` — so the invariant is checked BEFORE `createAccount`, never after: an invalid `provider` command must never create an orphaned Auth account. Shared error types added to `domain/identidad.errors.ts` per 4b.6, landed in this commit since they're this use case's own compensation-path errors: `EmailYaRegistradoError`, `AuthProviderError`, `RegistroFallidoError`. `UsuarioRegistrado` event added under new `domains/identidad/events/` folder — design.md's file-naming convention lists it as a sibling of `domain/`, not under `adapters/events/`; task 4c.4 mentions `adapters/events/` for the HTTP-adapter PR, read as the future event-subscriber layer, not the publish call site, since design.md's own sequence diagram shows the use case itself calling `publish()`. No logger port exists at this layer, so the orphan-compensation-failure path uses `console.error` (not `@nestjs/common`'s `Logger`) to keep `ports-in` framework-import-free per core-api-hexagonal-layout.)
- [x] 4b.2 Unit tests, `RegistrarUsuarioUseCase`: success path; deterministic profiles-failure→`deleteAccount`+503 no retry; ambiguous auth-failure→`findAccountByEmail`→forward-recovery, never delete; deterministic auth-failure (`email_taken`→409, other→502), zero compensation. (9 tests total: the 6 above plus invariant-rejected-before-Auth-call, compensation-itself-fails-still-throws-and-logs-orphan, and forward-recovery-found-retries-insert. `AuthProvider`/`ProfileRepository`/`EventPublisher` all mocked, zero DB.)
- [x] 4b.3 `RegistrarEmpresaUseCase`: single `companies` insert, never touches `profiles`, no audit write + unit test. (`id` generated app-side via `node:crypto randomUUID()` — `CompanyRepository.save` always needs an explicit id, matching the PR6 Kysely adapter's upsert shape. Publishes `EmpresaRegistrada` per `identidad/SPEC.md`'s "Eventos que publica" list — the corrected `core-api-identidad` spec doesn't restate this event explicitly, flagged as a spec-interpretation risk in the PR7 report for reviewer visibility.)
- [x] 4b.4 `AprobarEmpresaUseCase`/`SuspenderUsuarioUseCase`/`SuspenderEmpresaUseCase`: mutation + `AuditLogPort.record` inside one `runInTransaction`, event publishes post-commit + unit tests incl. rollback-removes-audit-entry. (All 3 `findById`/`findByProfileId` first — not just a not-found guard: `CompanyRepository.save` is an unconditional upsert, so calling it on a missing id would silently INSERT a bogus row instead of failing; the fetched row also supplies the audit entry's `cambios.status.antes`. "Rollback removes audit entry" proven at the unit level as "the mock proves `save`/`update` failing means `AuditLogPort.record` and `EventPublisher.publish` are never called" — real Postgres ROLLBACK behavior is `TransactionManager`'s own concern, already covered by PR3's integration test, not re-proven per use case.)
- [x] 4b.5 `AsignarRolAdminUseCase(profileId, rol, adminId)`: `adminId`→`granted_by`; `AdminRoleRepository.upsert` + audit in same transaction + unit test (re-grant replaces, not duplicates). (`findByProfileId` first, purely to supply the audit entry's `cambios.rol.antes` — `null` on a first grant. No event: `identidad/SPEC.md`'s "Eventos que publica" list does not name one for this use case.)
- [x] 4b.6 Shared domain error types (`RegistroFallidoError`, etc.) used across the 6 use cases. (`domain/identidad.errors.ts`, all 5 landed together in the 4b.1/4b.2 commit as one shared-file edit: `EmailYaRegistradoError`/`AuthProviderError`/`RegistroFallidoError` (used by `RegistrarUsuarioUseCase`) plus `CompanyNotFoundError`/`ProfileNotFoundError` (used by this commit's 3 mutation use cases). All are plain `Error` subclasses, no HTTP status attached per core-api-hexagonal-layout; the intended status-code mapping is documented per-class in a comment for Phase 4c's mapper to consume.)
- [x] 4b.7 (beyond this task's literal scope, added because `identidad.module.ts` cannot eagerly instantiate `RegistrarUsuarioUseCase` without a real `AUTH_PROVIDER` provider) `adapters/persistence/supabase-auth.provider.ts`: `SupabaseAuthProvider implements AuthProvider` over `supabase-js`'s Auth Admin API (`SUPABASE_CLIENT`, Auth-Admin-only per D-A). `createAccount`/`deleteAccount` are direct wrappers; `findAccountByEmail` (used only by the low-frequency forward-recovery path) scans one `listUsers({ perPage: 1000 })` page client-side — documented limitation, no server-side email filter exists in this SDK version, revisit if the orphan-recovery rate in production ever needs full pagination. `createAccount` failure classification: any response with a 4xx `status` is `AuthProviderDeterministicError` (`email_exists`/`user_already_exists` → `'email_taken'`, `invalid_credentials` → `'invalid_credentials'`, else `'other'`); no `status` (network failure) or 5xx → `AuthProviderAmbiguousError`. Lives under `adapters/persistence/` (not a new top-level folder) — `core-api-hexagonal-layout`'s "fixed folder shape" enumerates only `http|persistence|events`; flagged as a spec-clarification opportunity for a future `sdd-spec` pass (e.g. broaden `persistence` to cover non-SQL outbound integrations explicitly, or add a 4th category once another domain needs one). `identidad.module.ts` now binds `AUTH_PROVIDER` and wires `RegistrarUsuarioUseCase`; verified via a real compiled boot against local Supabase (`node dist/main.js`, `authenticator` role) — `IdentidadModule dependencies initialized` with zero unresolved-provider errors, `GET /health` → 200.

## Phase 4c: Identidad — HTTP adapter — Spec: `core-api-identidad`, `core-api-hexagonal-layout`

- [x] 4c.1 6 DTOs (`class-validator`+`@ApiProperty`) under `adapters/http/dto/`. (`registrar-usuario.dto.ts`, `profile-response.dto.ts`, `registrar-empresa.dto.ts`, `company-response.dto.ts`, `suspension.dto.ts` (shared by `suspenderUsuario`/`suspenderEmpresa` — identical `{motivo}` shape), `asignar-rol-admin.dto.ts`. `RegistrarUsuarioDto.role` deliberately narrows to `'user'|'provider'` — excludes `'admin'` — unlike `RegistrarUsuarioCommand.role: Role` which accepts all 3; flagged as a DTO-level defense-in-depth restriction not stated explicitly by `core-api-identidad`'s HTTP surface table (see this PR's report for the full rationale). `password` uses `@MinLength(8)`, a pragmatic default — no password policy is documented anywhere in this change's specs. Response DTO fields reuse `@repon/types`' `Role`/`ProfileStatus`/`CompanyStatus` per D3, no re-declaration.)
- [x] 4c.2 `identidad.mapper.ts`: DTO↔command, entity↔response DTO. (4 pure functions, explicit field-by-field mapping — no spread shortcuts, so a future DTO/command field drift is a compile error, not a silent pass-through. Unit-tested in `identidad.mapper.spec.ts`, incl. the "absent optional stays `undefined`, never coerced to `null`" case.)
- [x] 4c.3 `identidad.controller.ts`: 6 routes per design.md's table (`@Public()` on register-user/register-company; `@AdminRoles('super_admin','soporte')` on aprobación/suspensiones; `@AdminRoles('super_admin')` on rol-admin); controller passes scalars (`actor.profileId`) to use cases, never the `AuthenticatedActor` object. (Every route uses `ParseUUIDPipe` on `:id`; full `@nestjs/swagger` annotations per route incl. `@ApiBearerAuth()` — `main.ts`'s `DocumentBuilder` gained `.addBearerAuth()`, a small justified addition beyond this task's literal file list, so Swagger UI's "Authorize" button actually works for the 4 protected routes.)
- [x] 4c.4 Domain events are already published — `RegistrarUsuarioUseCase`/`RegistrarEmpresaUseCase`/`AprobarEmpresaUseCase`/`SuspenderEmpresaUseCase`/`SuspenderUsuarioUseCase` all call `EVENT_PUBLISHER.publish(...)` directly (PR7, `ports-in/`), matching design.md's own sequence diagram. No `adapters/events/` folder is created in this PR: per PR7's note, that folder (if ever built) would be an inbound event-*subscriber* layer, and `identidad/SPEC.md`'s "Eventos que consume: Ninguno" means there is nothing for it to subscribe to. Flagged (same as PR7's 4th-adapter-category note) as a `core-api-hexagonal-layout` clarification opportunity: is `adapters/{http,persistence,events}/` a fixed triad every domain must have (even empty), or conditional like `contracts/`? This PR takes the latter reading.
- [x] 4c.5 `identidad.module.ts`: wire the 6 use cases + controller into the module built in 4a. (Added `controllers: [IdentidadController]`; use cases were already wired in 4b.7.)
- [x] 4c.6 E2e tests for the 6 routes: DTO rejection (extra field→400), guard interplay per route's `@AdminRoles`, happy path per use case. (`test/identidad.e2e-spec.ts`, 14 tests against the REAL `AppModule`/`AuthGuard`/`RolesGuard`/`IdentidadExceptionFilter` — only the 6 identidad ports-out + `ACTOR_PORT` are overridden via `overrideProvider`, so no local Supabase is required in CI. Covers: both `@Public()` routes reachable with no `Authorization` header + happy path; extra-field→400 and missing-field→400; a protected route rejecting no-token with 401 `MISSING_BEARER_TOKEN`; a non-admin actor rejected with 403 `ROLE_NOT_ALLOWED`; `ADMIN_SUBROLE_MISSING`; `core-api-identidad` spec's own "soporte cannot assign admin roles" scenario -> 403 `ADMIN_SUBROLE_NOT_ALLOWED`; 3 admin happy paths (aprobación, suspensión, rol-admin); 2 domain-error-to-HTTP mappings (409 `EMAIL_YA_REGISTRADO`, 404 `COMPANY_NOT_FOUND`) proven through the real HTTP pipeline, not just the unit-level `identidad-exception.filter.spec.ts`. Also added beyond this task's literal scope: `identidad-dto.spec.ts` (18 tests, all 6 DTOs' validation rules incl. `forbidNonWhitelisted`) and `identidad-exception.filter.spec.ts` (6 tests, one per mapped domain error class) — both `test:unit`, DI-free, fast.)

## Phase 5: Closure — Spec: `core-api-bootstrap`, `repo-toolchain`

- [x] 5.1 5 placeholder modules (`catalogo`,`consumo`,`refill-matching`,`ofertas`,`pedidos-pagos`): folder skeleton + real DI tokens from each domain's own `SPEC.md`, no `useValue: {}`, registered in `app.module.ts`. (Each domain gets `ports-out/*.port.ts` (interface + `Symbol` token, `tx?: TransactionContext` trailing param matching the repo-wide convention, type-only `@repon/types` imports) plus a `<domain>.module.ts` that is a literal empty `@Module({})` — design.md's own words: "Los 5 módulos placeholder declaran tokens e interfaces pero NO los bindean... Prohibido `useValue: {}`". Tokens per domain (design.md's DI-wiring table): `catalogo` → `CATALOG_REPOSITORY`, `CATALOG_QUERY_PORT`; `consumo` → `CONSUMPTION_REPOSITORY`, `CONSUMPTION_LOG_REPOSITORY`; `refill-matching` → `REFILL_REPOSITORY`; `ofertas` → `OFFER_REPOSITORY`; `pedidos-pagos` → `ORDER_REPOSITORY`. `EventPublisher`/`NotificationPort`/`PaymentGatewayPort` are NOT redeclared per domain — already shared-kernel tokens (`EVENT_PUBLISHER`/`NOTIFICATION_PORT`/`PAYMENT_GATEWAY_PORT`, PR 5). Flagged deviation: `catalogo`'s `CatalogQueryPort` (the sync cross-domain query port `refill-matching`/`ofertas` will consume) is placed in `ports-out/` for now, not `contracts/` — `core-api-hexagonal-layout`'s own rule says only `contracts/` is cross-domain-importable, but this thin-placeholder PR (D2) doesn't create `contracts/` folders for any of the 5 domains and nothing imports it yet (the consuming domains are placeholders too, in this same PR); the file's own doc comment flags this as a placement gap to resolve when `catalogo` gets its real implementation.)
- [x] 5.2 Confirm the app boots with all 7 modules resolved, zero unresolved provider. (Verified twice: (a) `test/health.e2e-spec.ts` boots the real `AppModule` via `Test.createTestingModule` — still green, now exercising all 6 domains + kernel; (b) a real compiled boot, `node dist/main.js` against local `supabase start`, log shows `SharedKernelModule`/`CatalogoModule`/`ConsumoModule`/`RefillMatchingModule`/`OfertasModule`/`PedidosPagosModule`/`IdentidadModule` all "dependencies initialized", zero DI errors; `GET /health` → 200, `GET /api/docs` → 200, unmapped route → 404, a protected route with no token → 401 `MISSING_BEARER_TOKEN` (guard still active with all 6 domains wired).)
- [x] 5.3 `services/core-api/SPEC.md` delta: folder convention, cross-domain import rule, testing policy. (New "Delta implementado por `backend-core-api-foundation` (tasks.md fase 5.3)" section: per-domain folder shape table, the contracts/-only cross-domain-import rule + its ESLint `import-x/no-restricted-paths` enforcement (ESLint config reads `src/domains/` from disk, so it auto-covers all 6 domains including the 5 new placeholders — verified live: a temporary disallowed `catalogo` -> `consumo/ports-out` import was added, `npx eslint` failed with the expected "Only domains/consumo/contracts/ is importable..." message, then reverted), a testing-policy table (unit/e2e/integration, what runs in CI), and the "`AuthenticatedActor` never crosses `ports-in`" rule. Also added "Auditoría" to the "Infraestructura compartida" bullet list — it was missing despite `shared/audit/` existing since PR 4.)
- [x] 5.4 `services/core-api/domains/identidad/SPEC.md` delta: compensation ownership (D-B), `ProfileRepository` split, `AdminRoleRepository`, `tx?` on ports-out, `AuditLogPort` consumption. (Rewrote "Puertos de entrada"/"Puertos de salida" ts blocks to match the actual implemented shapes verbatim: `asignarRolAdmin(profileId, rol, adminId)`; `ProfileRepository.insertIfAbsent`+`update` replacing the old single `save`; new `AdminRoleRepository` interface (`upsert`+`findByProfileId`); every repository method carries `tx?: TransactionContext` except `AuthProvider`/`EventPublisher` (documented why); `AuditLogPort` consumption note — 4 admin use cases audit inside the same transaction, `registrarUsuario`/`registrarEmpresa` never do. Rewrote the compensation-contract section to fix the D-B documentation bug explicitly: the ORIGINAL prose said "the adapter must call `deleteAccount`", contradicting the diagram it cites — corrected to "the use case orchestrates the saga, the adapter only classifies the failure", with the rationale (hexagon-inversion risk) spelled out, not silently rewritten.)
- [x] 5.5 `packages/types/SPEC.md` delta: confirm `schema.ts` row types never live here (D-A). (Already satisfied — PR 2 / task 1.3's "reframe intro as documentation of the code" already added the exact "Regla de borde (D-A, no negociable)" paragraph stating Kysely row types never export from `@repon/types`. Verified via `git log -- packages/types/SPEC.md` (only touched by PR 2's `d8d72d9`) and a fresh read of the file — no further edit needed, this task closes as "confirmed pre-existing", not "newly written".)
- [x] 5.6 LAST TASK: `openspec/config.yaml` — `testing.status: configured`, real `test_command`/`build_command`/`lint_command`/`typecheck_command`, `strict_tdd: true` (D7) — only after `pnpm test` is verified green locally and in CI. (Flipped only after every other verification in this PR's report was green: root `pnpm typecheck`/`pnpm lint`/`pnpm test`/`pnpm build` all pass, plus the live full-app boot above. `testing.status: configured`, `reason` rewritten to describe the real toolchain instead of the pre-scaffolding vacuum, all 5 command fields filled (`pnpm test`/`pnpm build`/`pnpm lint`/`pnpm typecheck`/`pnpm format:check`), `coverage_threshold` left at `0` deliberately (coverage collection exists, no threshold enforced yet — not silently omitted, stated in `reason`). Also updated `rules.apply.tdd: true`+`test_command`, `rules.verify.test_command`/`build_command`, and the stale `context`/`rules.tasks` prose that still said "no test runner exists" / "Testing: NOT YET CONFIGURED" — both false as of this PR, `rules.tasks` narrowed to apply only to sub-projects that still lack one (`apps/*`, `packages/ui`).)

---

## Dependency Notes

Slices are strictly sequential — each depends on the previous being merged (kernel needs boot's `env.schema.ts`; `identidad` needs kernel's `ACTOR_PORT` contract and `AuditLogPort`; closure needs all 6 domain tokens fixed). Per this project's DoD: implementation + its unit/e2e tests + the relevant `SPEC.md` delta land in the **same commit/PR** — never split code from its test. Phase 0 is a hard prerequisite for everything after it.
