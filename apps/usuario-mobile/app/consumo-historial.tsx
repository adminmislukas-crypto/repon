import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-consumo-historial` — adherencia y racha del consumo. */
export default function ConsumoHistorialScreen() {
  return <ScreenStub title="Adherencia" description="Racha y porcentaje de adherencia del mes." />;
}
