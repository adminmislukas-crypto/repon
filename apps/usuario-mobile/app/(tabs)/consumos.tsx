import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-consumo` — dosis/porciones de hoy, con tabs para alternar
 *  entre el usuario y cada mascota. */
export default function ConsumosScreen() {
  return (
    <ScreenStub
      title="Consumos"
      description="Dosis/porciones de hoy, con tabs para alternar entre el usuario y cada mascota."
    />
  );
}
