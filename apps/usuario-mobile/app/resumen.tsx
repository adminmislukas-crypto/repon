import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-resumen` — resumen del pedido tras aceptar una oferta, antes
 *  de pagar. Presentaciones alternativas (`alt-flag`) se marcan visualmente. */
export default function ResumenScreen() {
  return (
    <ScreenStub
      title="Resumen del pedido"
      description="Proveedor, items, presentaciones alternativas marcadas, total antes de pagar."
    />
  );
}
