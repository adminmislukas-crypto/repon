import { ScreenStub } from '@/components/ScreenStub';

export default function CargaMasivaScreen() {
  return (
    <ScreenStub
      title="Carga masiva"
      description="Carga de catálogo vía .xlsx/.csv, hasta 500 productos."
    />
  );
}
