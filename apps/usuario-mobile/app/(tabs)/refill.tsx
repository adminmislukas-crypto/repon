import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-buscar` → `s-refill-builder` — buscar productos y armar la
 *  solicitud de refill en un mismo flujo. */
export default function RefillScreen() {
  return (
    <ScreenStub
      title="Refill"
      description="Buscar productos y armar la solicitud de refill en un mismo flujo."
    />
  );
}
