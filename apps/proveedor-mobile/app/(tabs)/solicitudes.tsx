import { ScreenStub } from '@/components/ScreenStub';

export default function SolicitudesScreen() {
  return (
    <ScreenStub
      title="Solicitudes"
      description="Todas las solicitudes disponibles, separadas en proactivas (usuarios próximos a refill) y reactivas (solicitudes abiertas)."
    />
  );
}
