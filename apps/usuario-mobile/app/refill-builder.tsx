import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-refill-builder` — continuación de la pestaña Refill
 *  (`s-buscar`): arma la solicitud con los productos elegidos y la envía. */
export default function RefillBuilderScreen() {
  return (
    <ScreenStub
      title="Crear refill"
      description="Productos elegidos, cantidad, y solicitar ofertas a proveedores."
    />
  );
}
