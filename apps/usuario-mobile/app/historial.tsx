import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-historial` — listado completo de pedidos, accedido desde Perfil. */
export default function HistorialScreen() {
  return <ScreenStub title="Historial de pedidos" description="Listado completo, agrupado por mes." />;
}
