import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-home` — resumen del mes, alertas de stock, ahorro acumulado,
 *  banner de ofertas proactivas recibidas. */
export default function DashboardScreen() {
  return (
    <ScreenStub
      title="Dashboard"
      description="Resumen del mes, alertas de stock, ahorro acumulado, banner de ofertas proactivas recibidas."
    />
  );
}
