import { ScreenStub } from '@/components/ScreenStub';

export default function AgregarProductoScreen() {
  return (
    <ScreenStub
      title="Nuevo producto"
      description="Alta individual al catálogo: precio base, precio máximo de oferta, stock y disponibilidad."
    />
  );
}
