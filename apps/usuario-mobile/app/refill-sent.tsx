import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-refill-sent` — confirmación de que la solicitud de refill se envió. */
export default function RefillSentScreen() {
  return (
    <ScreenStub title="Solicitud enviada" description="¡Refill solicitado! Esperando ofertas." />
  );
}
