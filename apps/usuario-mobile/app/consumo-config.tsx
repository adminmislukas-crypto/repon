import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-consumo-config` — gestión de los consumos activos del usuario/mascota. */
export default function ConsumoConfigScreen() {
  return <ScreenStub title="Configurar consumo" description="Consumos activos, editar o pausar." />;
}
