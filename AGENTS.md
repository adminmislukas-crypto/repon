# AGENTS.md

## Project Skills

| Skill | Path | Purpose |
|---|---|---|
| `repon` | `.claude/skills/repon/SKILL.md` | `/repon up`, `/repon down`, `/repon reset` — start/stop/reset the full local dev stack (Supabase, core-api, usuario-mobile, proveedor-mobile) for manual testing. |
| `repon-logs` | `.claude/skills/repon-logs/SKILL.md` | `/repon-logs` or "revisa los logs" — analyze `.dev/repon/*.log` for core-api/usuario-mobile/proveedor-mobile, group errors by service, propose fixes with file:line. |
