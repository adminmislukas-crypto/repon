node_modules/
.env
.env.*
!.env.example
.DS_Store
dist/
build/
.next/
coverage/
*.log
.turbo/

# Supabase CLI local artifacts (also covered by supabase/.gitignore,
# repeated at root so `git status` stays clean regardless of cwd)
supabase/.branches/
supabase/.temp/
