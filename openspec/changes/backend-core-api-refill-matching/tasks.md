# Tasks: `refill-matching` — cuarto vertical, primer consumidor de un `contracts/` ajeno, primera entidad con estado incompleto

Expands design.md's finalized **7-PR chained sequence** (§"Secuencia de implementación") into checkable, test-first tasks. Order, PR boundaries, and rationale are design.md's (D1-D17 from proposal.md, D-A..D-G.4 from design.md) — not re-derived here. The spec/design reconciliation table at the end of design.md (§"Reconciliación con `specs/`") is already applied to the 3 spec files under `specs/`; this document does not re-litigate it.

**Pre-split per the `ask-on-risk` delivery strategy**: design.md's own PR4 (creación) and PR5 (matching) each bundle a transactional/event-publishing use case with a first-of-its-kind HTTP surface (controller + DTOs + exception filter) — the same "logic vs. adapters" shape that made `consumo`'s PR2 exceed budget. Both are pre-split into `a`/`b` here, mirroring `consumo`'s `2a`/`2b`. Design.md's own PR6 ("auto + cierre") is pre-split into `6a` (listener + `crearBorradorRefill`) and `6b` (`completarBorrador`) exactly as design.md's §"Secuencia de implementación" itself recommends ("el 6 es el candidato #1 a partirse en 6a... y 6b..."). **7 PRs become 10** — every dependency edge below is unchanged from design.md; only the granularity of PR4, PR5, and PR6 changed.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~2,900-3,600 total, across **10** chained PRs; per-PR range 230-410 |
| 400-line budget risk | **Medium** — no PR's forecast clearly and unambiguously exceeds 400; four (PR1, PR3, PR6a, PR6b) sit at or just under the line on their upper bound and are the ones to watch during `sdd-apply` |
| Chained PRs recommended | Yes — 10 sequential PRs, dependency-ordered, cannot parallelize (each depends on the previous domain layer) |
| Chain strategy | stacked-to-main (each PR merges to `main` in order), same as `catalogo`/`consumo` |
| Delivery strategy | ask-on-risk (cached this session) — **not yet resolved by a maintainer**; this forecast is `sdd-tasks`' recommendation, not a confirmed decision |

```text
Decision needed before apply: Maybe — see "Borderline PRs" below. None of the 10 PRs is forecast to blow past 400 outright (unlike consumo's original 2/6, which combined two full concerns each); the 4 borderline ones are single, cohesive, already-minimal review units. Recommendation: proceed as-is unless actual diffs during apply run meaningfully over the estimates below, in which case use the named fallback split for that specific PR.
Chained PRs recommended: Yes
Chain strategy: stacked-to-main, 10 PRs
400-line budget risk: Medium
```

### Per-PR estimate

| PR | Slice | Est. lines | Risk | Note |
|---|---|---|---|---|
| 1 | 0 · groundwork | 320-400 | Medium | 2 migrations (D-A) + 2 row types (D10) + full `@repon/types` rewrite (D11/D-B) + finalized `RefillRepository` (D-G.2) + 6 error classes + the D-B compile-time type test. Pure costuras, zero behavior — same class as `catalogo`'s D13/`consumo`'s D12 groundwork PRs. **Fallback split if it runs over**: PR1a (2 migrations + `schema.ts` row types — pure DB layer) / PR1b (`@repon/types` rewrite + type-test + repo port + `refill.errors.ts` — pure TS layer) |
| 2 | 1 · dominio | 260-340 | Low | Factories `crearSolicitudActiva`/`crearBorrador`, `completar()` transition with D4's completeness invariant, 4-state machine. Jest puro, sin contenedor Nest — mirrors `consumo`'s 2a |
| 3 | 2 · persistencia | 320-410 | Medium | `KyselyRefillRepository`'s 4 methods (D-G.2) in one class, with the `Number(null) === 0` mapper callout (design.md's highest mechanical-risk item). Single reviewable adapter — matches `consumo`'s PR2b/PR4 precedent of staying whole at similar size. **Fallback split if it runs over**: PR3a (`save`+`findById`, what `crearSolicitud`/`completarBorrador` need) / PR3b (`findBorradorByConsumption`+`actualizarEstado`, what the listener/`marcarComo*` need) |
| 4a | 3 · creación (lógica) | 210-260 | Low | `CrearSolicitudUseCase` transactional (D14) + `comuna` (D12) + dueño del actor (D13); `RefillSolicitudPayload`/`RefillCreado`. Zero HTTP — mockable, testeable en aislamiento |
| 4b | 3 · creación (HTTP) | 270-330 | Low-Medium | Controller + DTOs + mapper + `RefillExceptionFilter` (**first creation** of this domain's filter/controller — same first-HTTP-PR cost `consumo`'s 2b absorbed) + `POST /refill/mis-solicitudes` + e2e. Depends on 4a |
| 5a | 4 · matching (lógica) | 270-320 | Medium | `BuscarProveedoresCompatiblesUseCase` — **the use case that closes R2 (404 cross-tenant) and R3 (no `TRANSACTION_MANAGER`)**, design.md's own "PR que más merece review dedicada". `MatchEncontrado`/payload. Zero HTTP |
| 5b | 4 · matching (HTTP) | 200-250 | Low | `POST .../matching` route + `ProveedorCompatibleDto` + filter extension (409/503) + e2e. Depends on 5a |
| 6a | 5 · auto | 330-410 | Medium | `CrearBorradorRefillUseCase` (D-G.1) + dedup (D-D.3) + `consumo-event.payloads.ts` + `RefillAutoSolicitadoListener` (D2/D8) + the `moduleRef.init()` contract e2e. Concentrates 2 of the 4 D17-mandatory tests. **Fallback split if it runs over**: 6a-i (use case + dedup, no listener) / 6a-ii (listener + payload types + `moduleRef.init()` e2e) |
| 6b | 5 · completar | 330-410 | Medium | `CompletarBorradorUseCase` (delegates the completeness check to Phase 2's `completar()`, already tested) + DTOs + `POST .../completar` + filter extension + e2e. **Fallback split if it runs over**: 6b-i (use case, logic only) / 6b-ii (DTOs+controller+filter-ext+e2e) |
| 7 | 6 · cierre | 260-360 | Low-Medium | `marcarComoOfertada`/`marcarComoConfirmada` (D6, no caller/no route — trivial `actualizarEstado` wrappers) + the raw `refill-matching/SPEC.md` rewrite + `packages/types/SPEC.md` delta + `docs/ARCHITECTURE.md` D9 correction + full-workspace verification. Docs carry lower per-line review cost than logic |

### Suggested Work Units

| Unit | Goal | PR | Base | Notes |
|---|---|---|---|---|
| 1 | Groundwork: 2 migraciones, row types, `@repon/types`, repo final, errores | PR 1 | `main` | Sin dependencias; bloquea todo lo demás. `pnpm typecheck` es el juez de si D-B rompe `catalogo` |
| 2 | Dominio: factories + invariante + máquina de estados | PR 2 | `main` | Depende de PR 1's tipos/errores. Cero I/O |
| 3 | Persistencia: `KyselyRefillRepository` (4 métodos) | PR 3 | `main` | Depende de PR 2's entidades para tipar el mapper de vuelta |
| 4a | `crearSolicitud` transaccional + `RefillCreado` | PR 4a | `main` | Depende de PR 3's `save` | 
| 4b | HTTP de creación | PR 4b | `main` | Depende de 4a |
| 5a | `buscarProveedoresCompatibles` (sin `TRANSACTION_MANAGER`) + `MatchEncontrado` | PR 5a | `main` | Depende de PR 3's `findById`. **Después de 4a/4b a propósito** (design.md): con la ruta transaccional ya cerrada, la ausencia de `TRANSACTION_MANAGER` acá es visible en el diff |
| 5b | HTTP de matching | PR 5b | `main` | Depende de 5a |
| 6a | Listener + `crearBorradorRefill` + dedup | PR 6a | `main` | Depende de PR 3's `findBorradorByConsumption`/`save` y PR 2's `crearBorrador` factory. Independiente de 4/5 salvo por el módulo compartido |
| 6b | `completarBorrador` | PR 6b | `main` | Depende de PR 2's `completar()`, PR 3's `save`, y **reutiliza `RefillCreado`/`RefillSolicitudPayload` de 4a** |
| 7 | Cierre | PR 7 | `main` | Depende de 6a y 6b — los deltas de SPEC.md describen comportamiento que ya debe existir |

---

## Phase 1: Groundwork — Spec: `db-schema-refill-matching`, `shared-types-package`, `core-api-refill-matching` (port final form)

- [x] 1.1 Write `supabase/migrations/20260807120000_14_refill_matching_estado_borrador.sql` — single statement `alter type public.refill_estado add value 'borrador' before 'abierta'`, with the full doc-comment rationale from design.md's "Batch 14" (why one statement only, why `BEFORE 'abierta'`, why not reversible — D-A/R8). **Verify against the actual latest file in `supabase/migrations/` before finalizing the timestamp** — at spec-writing time the latest applied migration is `20260806120000_13_consumo_stock_bajo_debounce.sql`, so `20260807...` is the next free day-slot; re-check this hasn't shifted since.
- [x] 1.2 Write `supabase/migrations/20260807120100_15_refill_matching_completitud_diferida.sql` — depends on 14 already committed (same day, next slot, mirrors the `09`/`10` and `11`/`12` two-slots-same-day pattern): `alter column direccion drop not null`, `alter column comuna drop not null` on `refill_requests`; `add column consumption_id uuid` (no FK — D-D.2); `alter column categoria drop not null`, `alter column precio_referencia drop not null` on `refill_items`; the 5 `comment on column` statements verbatim from design.md (D3/D4/D9/D-D.2 rationale, incl. the `Number(null) === 0` warning inline on `precio_referencia`); the partial unique index `refill_requests_borrador_por_consumo_uidx on (user_id, consumption_id) where estado = 'borrador' and consumption_id is not null` (D-D.2/D-D.3). Confirm `20260803120400_04_refill_matching.sql` is **not** edited (D9, fix-forward).
- [x] 1.3 Apply locally (`supabase start`/`db reset`); verify: `db-schema-refill-matching` Scenarios "A borrador row can be persisted alongside the 3 existing states", "Existing abierta/ofertada/confirmada rows are unaffected", "A borrador's items may omit categoria/precioReferencia", "No CHECK constraint enforces cross-table completeness", "A second RefillAutoSolicitado... is skipped" (index rejects a duplicate insert), "A completed request's consumption_id no longer blocks a new borrador", "consumption_id has no foreign key", "A borrador row is a valid state with comuna NULL". Confirm no grant/RLS changes needed (verified in design.md — table-level grants from batch `10` already cover new columns; RLS policies are column-agnostic).
- [x] 1.4 `shared/database/schema.ts`: add `RefillUrgenciaRow`, `RefillEstadoRow` (`'borrador' | 'abierta' | 'ofertada' | 'confirmada'`, same order as the enum post-batch-14), `RefillRequestsTable` (incl. `estado: Generated<RefillEstadoRow>`, `consumption_id: string | null`), `RefillItemsTable` (incl. `categoria: string | null`, `precio_referencia: string | null`); register both on `DB` (D10).
- [x] 1.5 Rewrite `packages/types/src/refill-matching.ts` per design.md D-B/D-D.2 verbatim: `Urgencia`, `RefillEstado` (`'borrador'` first), `RefillEstadoActivo = Exclude<RefillEstado, 'borrador'>`, `RefillItemCommon`/`RefillItem` (**unchanged shape** — `id`, `nombre`, `categoria: string`, `precioReferencia: number`, `catalogProductId?: string`), `RefillItemBorrador` (new sibling type, `categoria`/`precioReferencia` optional, no `?: never` — see the doc-comment rationale on why not, D-B "Desviación declarada"), `RefillRequestCommon` (incl. `consumptionId?: string`), `RefillRequestBorrador`, `RefillRequestActiva`, `RefillRequest = RefillRequestBorrador | RefillRequestActiva`, `NuevoRefillItem` (D11). **`RefillItem` itself must not change shape** — `shared-types-package` Scenario confirms this explicitly; if any diff here touches `RefillItem`'s existing 4 fields, stop and re-check against design.md D-B before proceeding.
- [x] 1.6 RED/compile-time: add a small type-only fixture (e.g. `services/core-api/src/domains/refill-matching/domain/refill-item-borrador.type-test.ts`) with a `// @ts-expect-error` line asserting a `RefillItemBorrador[]` value is NOT assignable where `RefillItem[]` is expected, using `CatalogQueryPort['buscarCoincidencias']`'s own parameter type (type-only import from `catalogo/contracts/catalog-query.port.ts` — no module wiring, no `CatalogoModule` import needed yet). **This is a TYPE-LEVEL test, checked by `tsc`/`pnpm typecheck`, not a runtime Jest assertion** — do not skip it as "not testable"; an unused `@ts-expect-error` directive fails `tsc` on its own, which is the pass/fail signal (`shared-types-package` Scenario "A borrador's items are never assignable where RefillItem[] is expected", design.md's testing table row "Tipos... `tsc`... Sí").
- [x] 1.7 GREEN: run `pnpm --filter @repon/types typecheck` and workspace-root `pnpm typecheck` — confirm 1.5's rewrite makes 1.6 pass, and confirm `catalogo`'s `KyselyCatalogQueryAdapter`/`catalog-query.port.ts` still compile untouched (D-B's whole point: `RefillItem`'s frozen shape is what keeps `catalogo` compiling with zero edits to `catalogo`'s own files).
- [x] 1.8 Rewrite `services/core-api/src/domains/refill-matching/ports-out/refill-repository.port.ts` to its final form (D-G.2): `save(request: RefillRequest, tx?)`, `findById(id, tx?)`, `findBorradorByConsumption(userId, consumptionId, tx?): Promise<RefillRequestBorrador | null>` (NEW), `actualizarEstado(id, estado: RefillEstadoActivo, tx?): Promise<void>` (NEW, narrow state-only write — never reschribes items, same reasoning `consumo`'s cron's CAS methods were forbidden from going through `save()`). Doc comments carry the D-G.2 rationale for both new methods.
- [x] 1.9 Create `services/core-api/src/domains/refill-matching/domain/refill.errors.ts`: `RefillRequestNotFoundError`, `SolicitudEnBorradorError`, `TransicionInvalidaError`, `SolicitudIncompletaError`, `RefillItemDesconocidoError`, `SolicitudInvalidaError` — 6 classes per design.md's error table (D-E), used incrementally by later PRs.
- [x] 1.10 Run `pnpm lint`/`pnpm typecheck` at workspace root — confirm the new interfaces compile with zero implementers yet (expected: nothing implements the extended `RefillRepository` until Phase 3).

## Phase 2: Dominio (puro, sin I/O) — Spec: `core-api-refill-matching`

Depends on Phase 1's types/errors. Zero adapters, zero I/O — testeable en aislamiento total.

- [x] 2.1 RED: `domain/refill-request.entity.spec.ts` — `crearSolicitudActiva(userId, items, direccion, comuna, urgencia)` rejects empty `items`, empty `direccion`/`comuna` (`SolicitudInvalidaError`); happy path returns `RefillRequestActiva` with `estado: 'abierta'` by construction, ids via `randomUUID()` for the request and every item (never a DB default — repo-wide precedent).
- [x] 2.2 GREEN: `domain/refill-request.entity.ts` — `crearSolicitudActiva()` factory.
- [x] 2.3 RED (extend the same file): `crearBorrador({ id, userId, consumptionId, urgencia: 'lo_antes_posible', items: [{ id, nombre }] })` (D-G.1) — no `direccion`/`comuna`/`categoria`/`precioReferencia` required or accepted; returns `RefillRequestBorrador` with `estado: 'borrador'` by construction.
- [x] 2.4 GREEN (extend the file): `crearBorrador()` factory.
- [x] 2.5 RED (extend the file): `completar(borrador, { direccion, comuna, urgencia?, items })` (D3/D4) — 3 negative scenarios FIRST (`core-api-refill-matching` "A borrador missing direccion or comuna is rejected", "A borrador with any item missing categoria or precioReferencia is rejected"): missing `direccion`, missing `comuna`, any item missing `categoria`/`precioReferencia` → `SolicitudIncompletaError`, `estado` stays `'borrador'` (return value untouched — no mutation on rejection). An unknown `refillItemId` in the input → `RefillItemDesconocidoError`. Happy path (`core-api-refill-matching` "A fully completed borrador transitions to abierta") → returns `RefillRequestActiva`, `estado: 'abierta'`, items updated in place (same array length/ids — never replaced, "cero DELETE físico" per design.md).
- [x] 2.6 GREEN (extend the file): `completar()` transition, delegating to the discriminated union so TypeScript — not just the runtime check — narrows the return type to `RefillRequestActiva`.
- [x] 2.7 RED (extend the file): 4-state machine — `marcarOfertada(activa)` (`'abierta' → 'ofertada'`) and `marcarConfirmada(ofertada)` (`'ofertada' → 'confirmada'`) reject an out-of-order call (e.g. confirming an `'abierta'` request) with `TransicionInvalidaError`.
- [x] 2.8 GREEN (extend the file): the 2 transition functions.

## Phase 3: Persistencia — Spec: `core-api-refill-matching`, `db-schema-refill-matching`

Depends on Phase 2's entities (return/parameter types).

- [x] 3.1 RED: `adapters/persistence/kysely-refill.repository.spec.ts` — `save()` on a NEW `RefillRequestActiva`: 1 insert into `refill_requests` with `estado` **always explicit in the insert statement**, never relying on the column's `default 'abierta'` (D-G.4 — write a dedicated assertion inspecting the built query/args, not just the end state, so no code path can silently start relying on the default); 1 bulk insert of N `refill_items` (single statement, not N roundtrips).
- [x] 3.2 GREEN: `adapters/persistence/kysely-refill.repository.ts` — `save()`'s insert path.
- [x] 3.3 RED (extend the file): `save()` on an EXISTING `RefillRequestBorrador → RefillRequestActiva` transition (the `completarBorrador` path): 1 update on `refill_requests`, items updated **in place** keyed by id — never a DELETE, never re-inserting existing item rows (criterio de éxito: "ningún DELETE físico introducido").
- [x] 3.4 GREEN (extend the file): `save()`'s update/upsert-items-in-place path.
- [x] 3.5 RED (extend the file): **the `Number(null) === 0` round-trip** (design.md's highest mechanical-risk callout) — a borrador row with `categoria: null`, `precio_referencia: null`, `direccion: null`, `comuna: null` in Postgres maps back through `findById()` to `categoria: undefined`, `precioReferencia: undefined`, `direccion: undefined`, `comuna: undefined` — asserted explicitly as `undefined`, **never `0`, never `''`**. A complete row's `precio_referencia: '1990.00'` (string from the driver, numeric OID) maps to `precioReferencia: 1990` (number).
- [x] 3.6 GREEN (extend the file): `findById()`'s mapper — the exact conditional conversion from design.md's callout (`row.precio_referencia === null ? undefined : Number(...)`, `row.categoria ?? undefined`), never a naive `Number(row.x)`.
- [x] 3.7 RED (extend the file): `findBorradorByConsumption(userId, consumptionId)` (D-D.3) — returns the open `RefillRequestBorrador` when one exists for that `(userId, consumptionId)` pair with `estado = 'borrador'`; returns `null` when none exists or the matching row has already transitioned out of `'borrador'` (`db-schema-refill-matching` "A completed request's consumption_id no longer blocks a new borrador").
- [x] 3.8 GREEN (extend the file): `findBorradorByConsumption()`.
- [x] 3.9 RED (extend the file): `actualizarEstado(id, estado)` (D-G.2/D6) — a single narrow `UPDATE ... SET estado = $2` statement; asserts it does **not** touch `refill_items` (no join, no cascading write) — the reason this method exists instead of routing `marcarComoOfertada`/`marcarComoConfirmada` through `save()`.
- [x] 3.10 GREEN (extend the file): `actualizarEstado()`.
- [ ] 3.11 Opt-in integration test (`supabase start` local, not CI): round-trip a borrador through real Postgres — `NULL` survives as `undefined`, never `0`; the partial unique index rejects a second concurrent insert for the same `(user_id, consumption_id)` while the first is still `'borrador'`.

## Phase 4a: Creación (lógica) — Spec: `core-api-refill-matching`

Depends on Phase 3's `save()`.

- [x] 4a.1 `events/refill-solicitud.payload.ts`: `RefillSolicitudItemPayload` (`refillItemId`, `nombre`, `categoria`, `precioReferencia`, `catalogProductId: string | null` — all required, never optional: these events only publish on an active request) + `RefillSolicitudPayload` (`refillRequestId`, `userId`, `comuna`, `urgencia`, `items[]` — **no `direccion`**, D-C privacy rule) verbatim from design.md.
- [x] 4a.2 `events/refill-creado.event.ts`: `RefillCreado implements DomainEvent`, `type = 'refill.creado'`, carries `RefillSolicitudPayload`.
- [x] 4a.3 RED: `ports-in/crear-solicitud.use-case.spec.ts` — `crearSolicitud(actor.profileId, items, direccion, comuna, urgencia)`: `userId` **always** from the caller-supplied `profileId` argument, never a client-supplied field (`core-api-refill-matching` "The actor's profileId becomes the owner"); `runInTransaction` wraps 1 `save()` call; a failure inside the transaction (mock `save` to reject) leaves nothing persisted and `publish` is **never called** (`core-api-refill-matching` "An item failure leaves no partial request", D14); `publish(RefillCreado)` fires **only after** the mocked transaction resolves, never before/inside (`core-api-refill-matching` "Request and items commit together"); a call missing `comuna` is rejected before any `save()` invocation (`core-api-refill-matching` "A manual crearSolicitud call without comuna is rejected", D12 — delegates to Phase 2's `crearSolicitudActiva` validation, asserted here as "no I/O attempted").
- [x] 4a.4 GREEN: `ports-in/crear-solicitud.use-case.ts` — constructs the entity via Phase 2's `crearSolicitudActiva`, `runInTransaction(tx => repository.save(entity, tx))`, `publish(new RefillCreado(payload))` after the transaction resolves.

## Phase 4b: Creación (HTTP) — Spec: `core-api-refill-matching`

Depends on Phase 4a. **First HTTP surface of this domain** — filter/controller/mapper are created here, not extended.

- [x] 4b.1 `adapters/http/dto/nuevo-refill-item.dto.ts` (`nombre`/`categoria` non-empty, `precioReferencia >= 0`, `catalogProductId?`), `adapters/http/dto/crear-solicitud.dto.ts` (`items: NuevoRefillItemDto[]` non-empty, `direccion`, `comuna`, `urgencia` in the enum — **no `userId` field**, D13), `adapters/http/dto/refill-request-response.dto.ts`.
- [x] 4b.2 `adapters/http/refill.mapper.ts`: `toRefillRequestResponseDto()`.
- [x] 4b.3 `adapters/http/refill.controller.ts` (NEW): `POST /refill/mis-solicitudes` — authenticated, no `@Roles()` (D-E rationale: `refill_requests.user_id` has no role restriction in RLS either); `actor.profileId` passed to the use case, never a body field; 201 response.
- [x] 4b.4 RED: `adapters/http/refill-exception.filter.spec.ts` (NEW) — `SolicitudInvalidaError` → 400 `SOLICITUD_INVALIDA` (the only error reachable from this route so far; other mappings extend this same file in later phases).
- [x] 4b.5 GREEN: `adapters/http/refill-exception.filter.ts` — mirrors `ConsumoExceptionFilter`/`CatalogoExceptionFilter` exactly: constructor-keyed map, `{ statusCode, code, message }` envelope, `@Catch()` scoped so it does not compete with the global filter.
- [x] 4b.6 `refill-matching.module.ts`: `imports: [DatabaseModule]` (not yet `CatalogoModule` — lands in Phase 5); bind `REFILL_REPOSITORY` → `KyselyRefillRepository`; register `CrearSolicitudUseCase`, `RefillController`, the filter; `exports: []` (D7).
- [x] 4b.7 E2e: `test/refill-crear-solicitud.e2e-spec.ts` — 201 happy path (request + N items persisted, `RefillCreado` observable on the real event bus after the response resolves); 400 on empty `items`/negative `precioReferencia`/missing `comuna`; 401 no token; DTO shape rejects a client-supplied `userId` field.

## Phase 5a: Matching (lógica) — Spec: `core-api-refill-matching`

Depends on Phase 3's `findById()`. **Después del 4a/4b a propósito** (design.md): con la ruta transaccional ya cerrada, la ausencia de `TRANSACTION_MANAGER` acá es visible en el diff.

- [ ] 5a.1 `events/match-encontrado.payload.ts`: `MatchEncontradoPayload extends RefillSolicitudPayload` + `companyIds: readonly string[]` (deduplicated, first-appearance order) + `providerCatalogItemIds: readonly string[]` — **no `ProviderCatalogItem` snapshot, no prices, no stock** (D-C Decisión 2).
- [ ] 5a.2 `events/match-encontrado.event.ts`: `MatchEncontrado implements DomainEvent`, `type = 'refill.match_encontrado'`.
- [ ] 5a.3 RED (the first negative, written FIRST per D17/R2): `ports-in/buscar-proveedores-compatibles.use-case.spec.ts` — request belongs to another user (or does not exist) → `RefillRequestNotFoundError` (byte-for-byte the same error either way, `core-api-refill-matching` "Cross-tenant read returns 404, not 403").
- [ ] 5a.4 RED (extend the file): a `'borrador'` request belonging to **another** user → **still** `RefillRequestNotFoundError` (404), never the borrador-specific error — ownership checked **before** state (`core-api-refill-matching` "Ownership is still checked before state, on a borrador request too", D-F's ordering rule).
- [ ] 5a.5 RED (extend the file): a `'borrador'` request belonging to the **caller** → `SolicitudEnBorradorError` (409), never `[]` (`core-api-refill-matching` "Matching on a borrador request is rejected explicitly", D-F). TypeScript narrows `RefillRequestActiva` past this point — note in the test/impl comment that a borrador's `RefillItemBorrador[]` items are structurally impossible to reach `buscarCoincidencias` from here (compile-time backstop, D-B; the runtime 409 is the HTTP-observable contract).
- [ ] 5a.6 RED (extend the file): **constructor-inspection test** — `Reflect.getMetadata('design:paramtypes', BuscarProveedoresCompatiblesUseCase)` (or the project's existing helper) does **not** contain `TRANSACTION_MANAGER`, mirroring `consumo`'s `ProcesarConsumosVencidosUseCase` test exactly (`core-api-refill-matching` "The matching use case has no transaction manager injected", D14/R3 — "the guarantee is not a comment or a convention to remember, it's the absence of the token in the constructor").
- [ ] 5a.7 RED (extend the file): `CatalogQueryPort.buscarCoincidencias` mocked to throw `CatalogQueryUnavailableError` → the use case does **not** catch it, it propagates uncaught to the caller (`core-api-refill-matching` "A catalog outage maps to 503, not an empty result" — the 503 mapping itself is the filter's job, Phase 5b; this test only proves the use case never swallows it into `[]`).
- [ ] 5a.8 RED (extend the file): happy path with 2 companies matching → `companyIds` deduplicated in first-appearance order, `providerCatalogItemIds` the full matched set, `MatchEncontrado` published with both.
- [ ] 5a.9 RED (extend the file): `buscarCoincidencias` returns `[]` → `MatchEncontrado` still publishes, with `companyIds: []` — **never suppressed** (`core-api-refill-matching` "A zero-match search still publishes MatchEncontrado", D-C).
- [ ] 5a.10 GREEN: `ports-in/buscar-proveedores-compatibles.use-case.ts` — constructor takes only `REFILL_REPOSITORY`, `CATALOG_QUERY_PORT`, `EVENT_PUBLISHER` (never `TRANSACTION_MANAGER`); `findById` → not-found/not-owner check → borrador check → `buscarCoincidencias(r.items)` **outside** any `runInTransaction` (there is none to be inside of) → dedup → `publish(MatchEncontrado)`.

## Phase 5b: Matching (HTTP) — Spec: `core-api-refill-matching`

Depends on Phase 5a.

- [ ] 5b.1 `adapters/http/dto/proveedor-compatible.dto.ts`: `ProveedorCompatibleDto` (maps `ProviderCatalogItem` → response shape).
- [ ] 5b.2 `adapters/http/refill.controller.ts`: `POST /refill/mis-solicitudes/:refillRequestId/matching` — `ParseUUIDPipe` on the param, authenticated, no `@Roles()`, 200. **`POST`, not `GET`** (D-E — the use case publishes an event, so it must not be safe/idempotent/cacheable).
- [ ] 5b.3 RED (extend `refill-exception.filter.spec.ts`): `RefillRequestNotFoundError` → 404 `REFILL_REQUEST_NOT_FOUND`; `SolicitudEnBorradorError` → 409 `REFILL_REQUEST_EN_BORRADOR`; `CatalogQueryUnavailableError` (**imported from `catalogo/contracts/catalog-query.port.ts`** — the one legitimate cross-domain import in this domain, per D15/C8) → 503 `CATALOG_UNAVAILABLE`.
- [ ] 5b.4 GREEN (extend the filter): the 3 new mappings.
- [ ] 5b.5 `refill-matching.module.ts`: add `CatalogoModule` to `imports` (the **first inter-domain module edge** in the repo, purely additive — consumes the already-exported `CATALOG_QUERY_PORT`, zero edits to any `catalogo` file); register `BuscarProveedoresCompatiblesUseCase`.
- [ ] 5b.6 E2e: `test/refill-buscar-proveedores.e2e-spec.ts` — 404 cross-tenant (own request of user A read as user B → 404); 404 on a borrador belonging to another user (order-of-checks proof); 409 on the caller's own borrador; 503 with `CATALOG_QUERY_PORT` mocked to throw; 200 happy path (`companyIds` non-empty); 200 with `companyIds: []` still returns 200 with an empty provider list (the event still fires — assert via a spy on the real event bus, not just the HTTP response).

## Phase 6a: Auto — Spec: `core-api-refill-matching`

Depende de Phase 3 (`findBorradorByConsumption`/`save`) y Phase 2 (`crearBorrador`). Independiente de 4/5 salvo el módulo compartido.

- [ ] 6a.1 RED: `ports-in/crear-borrador-refill.use-case.spec.ts` — dedup FIRST (`db-schema-refill-matching`/design.md D-D.3, "A second RefillAutoSolicitado for the same consumption while a borrador is open is skipped"): `findBorradorByConsumption` returns an existing open borrador → logs `refill.borrador_omitido` + returns, **zero** `save()` calls, **zero** `publish()` calls. Happy path: no existing borrador → `save()` called once inside `runInTransaction`, **zero events published** — a borrador insert publishes nothing (`core-api-refill-matching` "The listener's borrador creation publishes no event", D-C Decisión 1).
- [ ] 6a.2 GREEN: `ports-in/crear-borrador-refill.use-case.ts` — `execute({ consumptionId, userId, nombre })`: `runInTransaction(tx => { findBorradorByConsumption(userId, consumptionId, tx) → if found, log+return; else crearBorrador() (Phase 2 factory) → save(tx) })`.
- [ ] 6a.3 `adapters/events/consumo-event.payloads.ts`: a **local** interface (`RefillAutoSolicitadoPayload` — `consumptionId`, `userId`, `nombre`, the 3 fields this domain actually consumes, nothing else) — same literal pattern as `catalogo/adapters/events/identidad-event.payloads.ts`. Doc comment explains why: this domain never imports `consumo`'s real event class.
- [ ] 6a.4 RED: `adapters/events/refill-auto-solicitado.listener.spec.ts` — `@OnEvent('consumo.refill_auto_solicitado')` (channel-name STRING subscription, verified by asserting the decorator metadata or by the e2e in 6a.6 — not by importing `consumo`'s event class here). The mandatory D17 negative: the use case's `execute()` mocked to **reject** → `onRefillAutoSolicitado` still resolves without throwing, `logger.error` called (`core-api-refill-matching`/R5 "the listener captures and logs, never re-throws"). Also assert `refill-matching`'s `adapters/events/` has **no** `@OnEvent('consumo.stock_bajo_detectado')` handler anywhere in the domain (`core-api-refill-matching` "StockBajoDetectado alone creates nothing" — an enumeration/inspection assertion, not just "we didn't write one").
- [ ] 6a.5 GREEN: `adapters/events/refill-auto-solicitado.listener.ts` — `try { await crearBorradorRefillUseCase.execute(payload) } catch (error) { this.logger.error(...) }`, never re-throws. Mirrors `CompanyVisibilityListener`'s catch-and-log shape exactly.
- [ ] 6a.6 `refill-matching.module.ts`: register `CrearBorradorRefillUseCase` (providers only — internal, no route, no `@Roles()`, D-G.1) and `RefillAutoSolicitadoListener` (in `providers`, not `controllers` — `DiscoveryService` finds `@OnEvent` either way).
- [ ] 6a.7 E2e contract test: `test/refill-auto-solicitado.e2e-spec.ts` — **named `.e2e-spec.ts`, not `.contract-spec.ts`**: `catalogo`'s own PR8b discovered that `test/jest-e2e.json`'s `testRegex` only matches `.e2e-spec.ts$`, so a `.contract-spec.ts` file silently never runs in CI (see `test/catalogo-visibility.e2e-spec.ts`'s doc comment for the full incident). Real `EventEmitterModule.forRoot()`, real `EVENT_PUBLISHER`, real `RefillAutoSolicitadoListener`, **`await moduleRef.init()`, never only `.compile()`** — without `.init()`, `onApplicationBootstrap` never fires, `@OnEvent` never registers, and the test passes without testing anything (the exact bug `catalogo`'s PR8b caught; cite this in the file's own doc comment so it isn't rediscovered). Assertions: publishing a real `RefillAutoSolicitado`-shaped event on `consumo.refill_auto_solicitado` creates exactly one `'borrador'` `RefillRequest`; publishing the same payload twice (same `consumptionId`) creates only one; publishing an event on `consumo.stock_bajo_detectado` (same payload shape) creates **zero** requests (`core-api-refill-matching` "StockBajoDetectado alone creates nothing", D2/D17).

## Phase 6b: Completar — Spec: `core-api-refill-matching`

Depende de Phase 2's `completar()`, Phase 3's `save()`, y reutiliza Phase 4a's `RefillCreado`/`RefillSolicitudPayload`.

- [ ] 6b.1 In `ports-in/completar-borrador.use-case.ts`: declare `CompletarRefillItemInput` **locally** (`refillItemId`, `categoria`, `precioReferencia`, `catalogProductId?`) — **not** added to `@repon/types` (D-B "`CompletarRefillItemInput` NO va a `@repon/types`": `completarBorrador` isn't named in the raw `SPEC.md`, so it follows the `NuevaMascotaInput`/`NuevoConsumoInput` precedent of living inside its own `ports-in/*.use-case.ts` file, not the shared package). Keep `@repon/types`' export count at exactly the 4 from D11 — do not let this task silently add a 5th.
- [ ] 6b.2 RED: `ports-in/completar-borrador.use-case.spec.ts` — cross-tenant 404 FIRST (same shape as 5a.3): a borrador of another user → `RefillRequestNotFoundError`. A request that is not `'borrador'` (already `'abierta'`/`'ofertada'`/`'confirmada'`) → `TransicionInvalidaError` (409). An input covering a `refillItemId` that doesn't belong to the borrador → `RefillItemDesconocidoError` (400). Delegates completeness validation to Phase 2's `completar()` — do not re-implement the invariant here; mock/assert the delegation, don't duplicate the 3 negative scenarios Phase 2 already covers. Happy path: `runInTransaction` wraps `findById` (inside the tx, same pattern as `marcarDosisTomada`) + `save()`; `publish(RefillCreado)` fires **after** commit (`core-api-refill-matching` "completarBorrador publishes RefillCreado after commit").
- [ ] 6b.3 GREEN: `ports-in/completar-borrador.use-case.ts`.
- [ ] 6b.4 `adapters/http/dto/completar-refill-item.dto.ts`, `adapters/http/dto/completar-borrador.dto.ts` (`direccion`, `comuna`, `urgencia?`, `items: CompletarRefillItemDto[]` — must cover every item of the borrador, no more, no fewer).
- [ ] 6b.5 `adapters/http/refill.controller.ts`: `POST /refill/mis-solicitudes/:refillRequestId/completar` — 200, authenticated, no `@Roles()`.
- [ ] 6b.6 RED (extend `refill-exception.filter.spec.ts`): `TransicionInvalidaError` → 409 `TRANSICION_INVALIDA`; `SolicitudIncompletaError` → 400 `REFILL_REQUEST_INCOMPLETA`; `RefillItemDesconocidoError` → 400 `REFILL_ITEM_DESCONOCIDO`.
- [ ] 6b.7 GREEN (extend the filter): the 3 new mappings.
- [ ] 6b.8 `refill-matching.module.ts`: register `CompletarBorradorUseCase`.
- [ ] 6b.9 E2e: `test/refill-completar-borrador.e2e-spec.ts` — 200 happy path (borrador → `'abierta'`, `RefillCreado` observable on the event bus after commit); 400 missing `direccion`/`comuna`; 400 an item missing `categoria`/`precioReferencia`; 400 unknown `refillItemId`; 404 cross-tenant; 409 completing an already-`'abierta'` request.

## Phase 7: Cierre — Spec: all 3 delta specs + the raw domain `SPEC.md`

Depends on 6a and 6b — describes behavior that must already exist.

- [ ] 7.1 RED: `ports-in/marcar-como-ofertada.use-case.spec.ts` + `ports-in/marcar-como-confirmada.use-case.spec.ts` — each calls `actualizarEstado` with the expected target state; each constructor injects only `REFILL_REPOSITORY` (no `EVENT_PUBLISHER`, no `AuditLogPort` — D16); route-enumeration test confirming neither is reachable from `refill.controller.ts`'s registered routes (`core-api-refill-matching` "Neither method is HTTP-reachable") and neither is called from anywhere else in this change's module graph (`core-api-refill-matching` "No caller exists in this change", D6).
- [ ] 7.2 GREEN: `ports-in/marcar-como-ofertada.use-case.ts`, `ports-in/marcar-como-confirmada.use-case.ts`. Register both in `refill-matching.module.ts` `providers` (no controller wiring, D6) — confirm `exports: []` still holds (D7).
- [ ] 7.3 Rewrite `services/core-api/domains/refill-matching/SPEC.md`: `RefillInboundPort` signature gains `comuna` (D12) and drops the raw `userId`-as-parameter framing in favor of "owner derives from actor" prose (D13); `RefillRepository` interface gains `findBorradorByConsumption`/`actualizarEstado` (D-G.2); "Entidades que posee" notes the `'borrador'` 4th state (D3); "Eventos que publica" corrected — `RefillCreado` fires on the transition to `'abierta'`, `MatchEncontrado` carries `companyIds`/`providerCatalogItemIds`, **not** "notifica a los proveedores compatibles" via `RefillCreado` (D-G.3, the exact prose bug named in this change's brief); "Eventos que consume" drops `EmpresaSuspendida` (D5) and corrects `RefillAutoSolicitado` to say it triggers `crearBorradorRefill`, not `crearSolicitud` (D-G.1).
- [ ] 7.4 `packages/types/SPEC.md`: update the `src/refill-matching.ts` table row to list `Urgencia`, `RefillEstado`, `RefillItem`, `RefillItemBorrador`, `RefillRequest`, `NuevoRefillItem` (D11); correct the now-stale "`RefillRequest.comuna` siempre requerido" validation-rules bullet — `comuna` is required only for the active variant now (D4).
- [ ] 7.5 `docs/ARCHITECTURE.md`: correct line 42's "Edge Function" framing — matching runs in `core-api` via `buscarProveedoresCompatibles` + `CatalogQueryPort`, not an Edge Function, and this change does not filter by zone/comuna (D1/D9) — same declared-correction format as the D11 paragraph `consumo` already added below the cron section.
- [ ] 7.6 Audit `refill-matching.module.ts`: confirm `exports: []` (nothing crosses the boundary, D7), `imports: [DatabaseModule, CatalogoModule]` exactly, and that no file under `domains/catalogo/` was touched anywhere in this change's diff (D5's success criterion).
- [ ] 7.7 Confirm folder shape: `domains/refill-matching/` contains `adapters/events/` (exactly 1 `@OnEvent` listener) and **no** `contracts/`, **no** `adapters/scheduling/` (`core-api-refill-matching` "Folder shape matches D7/D8").
- [ ] 7.8 Full workspace verification: `pnpm lint`, `pnpm typecheck`, `pnpm test` (unit + e2e, incl. full regression run of `identidad`/`catalogo`/`consumo` suites — `catalogo` matters especially since D-B touches it by types, not files), `pnpm build`, `pnpm format:check`; opt-in integration suite against local Supabase (Phase 3's `Number(null)` round-trip + the partial-index concurrency proof).
- [ ] 7.9 Carry forward the residual risks/open questions from design.md's "Riesgos residuales y preguntas abiertas" (R4 borrador doesn't expire, `MatchEncontrado` not deduplicated across repeat calls, matching allowed on `'ofertada'`/`'confirmada'`, `refill_items` missing `updated_at` despite `completarBorrador` mutating it, the `catalogProductId`-on-borrador follow-up) as documented follow-ups — mirrors `consumo` tasks.md 7.9's precedent — none silently dropped.

---

## Dependency Notes

10 PRs, strictly sequential per design.md's dependency chain plus the pre-split boundaries: PR1 → PR2 → PR3 → PR4a → PR4b → PR5a → PR5b → PR6a → PR6b → PR7. Non-adjacent edges: PR6a depends on PR2/PR3 directly, not PR4/PR5 — it can in principle land before PR5 if a reviewer prefers, since nothing in 6a reads `CatalogoModule` or the matching use case; it is kept after 5b here only to match design.md's own PR ordering and because PR7's `refill-matching.module.ts` audit is simplest read against a single incremental history. PR6b depends on PR4a specifically (reuses `RefillCreado`/`RefillSolicitudPayload`), not PR4b/5a/5b. Per this project's DoD (`openspec/config.yaml`): implementation + its unit/e2e tests + the relevant `SPEC.md` delta land in the same commit/PR — Phase 7 is the exception by design, same as `catalogo`'s and `consumo`'s own closing phases. `strict_tdd: true` is active for every task introducing real logic — RED items are failing tests written first, GREEN items are the minimal implementation that passes them; 1.6/1.7 are the one exception (a type-level check, not a Jest RED/GREEN pair) and are labeled as such explicitly so they are not skipped.
