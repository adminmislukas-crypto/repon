import { ScreenStub } from '@/components/ScreenStub';

/** SPEC.md `s-inbox` — bandeja de ofertas recibidas de proveedores. */
export default function OfertasScreen() {
  return (
    <ScreenStub title="Ofertas" description="Bandeja de ofertas recibidas de proveedores." />
  );
}
