import { ScreenStub } from '@/components/ScreenStub';

export default function CatalogoScreen() {
  return (
    <ScreenStub
      title="Mi catálogo"
      description="Catálogo del proveedor por categoría, con precio base."
    />
  );
}
